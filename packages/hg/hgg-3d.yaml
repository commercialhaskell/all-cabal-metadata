all-versions:
- 0.1.0.0
author: Toshiaki Honda
basic-deps:
  aeson: '>=2.1 && <2.3'
  base: '>=4.17 && <5'
  bytestring: '>=0.11 && <0.13'
  containers: '>=0.6 && <0.8'
  directory: '>=1.3 && <1.4'
  filepath: '>=1.4 && <1.6'
  hgg-3d: '>=0'
  hgg-core: '>=0.1 && <0.2'
  hgg-frame: '>=0.1 && <0.2'
  hgg-pdf: ^>=0.1
  hgg-rasterific: ^>=0.1
  hgg-svg: '>=0.1 && <0.2'
  process: '>=1.6 && <1.7'
  text: '>=2.0 && <2.2'
  vector: '>=0.13 && <0.14'
changelog: |
  # Changelog for `hgg-3d`

  ## 0.1.0.0 — 2026-07-18

  First public release on Hackage.

  - 3D plotting (CPU projection): surface / scatter / bar / wireframe / quiver / trisurf; works with all 2D backends.
changelog-type: markdown
description: |-
  A 3D plotting library for hgg, comparable to matplotlib's mplot3d.
  CPU-based projection plus the painter's algorithm let the existing hgg
  2D backends (SVG / PDF / Rasterific) render 3D scatter / line /
  wireframe / surface plots as-is.
  .
  No WebGL required — pure Haskell end to end. Interactive 3D (camera
  rotation etc.) in web environments is handled by a separate
  PureScript / WebGL frontend outside this package, connected through the
  pre-bundled @data/webgl-spec.js@.
description-type: haddock
hash: 332793f060188c6979a47e61e44fbe45c90a443e2c72fb2bbe9d4523dd69459c
homepage: https://github.com/frenzieddoll/hgg
latest: 0.1.0.0
license-name: BSD-3-Clause
maintainer: frenzieddoll@gmail.com
synopsis: 3D plot (CPU projection) for hgg; works with all 2D backends
test-bench-deps:
  base: '>=0'
  directory: '>=0'
  hgg-3d: '>=0'
  hgg-core: '>=0'
  hgg-frame: '>=0'
  hspec: '>=2.10 && <2.12'
  text: '>=0'
  vector: '>=0'
