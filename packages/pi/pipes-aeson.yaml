homepage: https://github.com/k0001/pipes-aeson
changelog-type: markdown
hash: 71dd4aa8d7c181f9bbf7b608956588acca77cf6f33df12cc8f6cdd4894501220
test-bench-deps: {}
maintainer: renzocarbonaraλgmail.com
synopsis: Encode and decode JSON streams using Aeson and Pipes.
changelog: ! "# 0.4.1.3\n\n* Raise upper bound dependency on `attoparsec`.\n\n\n#
  0.4.1.2\n\n* Raise upper bound dependency on `aeson`.\n\n\n# 0.4.1.1\n\n* Raise
  upper bound dependency on `pipes-bytestring`.\n\n\n# 0.4.1\n\n* Raise upper bound
  dependency on `attoparsec`.\n\n\n# 0.4\n\n* Remove `Pipes.Aeson.encode` in favour
  of `encodeObject` and\n  `encodeArray`.\n\n* `decode` and `decodeL` now return `Nothing`
  on end of input, instead\n  of failing with a `DecodingError`. This follows the
  approach taken\n  by `pipes-attoparsec-0.5`.\n\n* Solved quadratic time complexity
  issue when decoding (#10).\n\n* Depend on `pipes-attoparsec-0.5.*`.\n\n* Raise upper
  bound for `transformers` to `0.4.*`.\n\n\n# 0.3.0\n\n* API revamped to be compatible
  with `pipes-parse-3.0.*`.\n\n* Renamed `Pipes.Aeson.Unsafe` module to `Pipes.Aeson.Unchecked`.\n\n\n#
  0.2.1\n\n* Generalize `encode` from `Producer` to `Producer'`.\n\n* Depend on newer
  versions of `aeson`, `attoparsec`, `pipes-attoparsec`.\n\n\n# 0.2.0\n\n* Version
  compatible with `pipes-4.0.0` and `pipes-parse-2.0.0`.\n\n* API radically changed.
  Removed `parseValue`, `fromValue` and\n  `TopLevelValue`. Other things renamed.\n\n\n#
  0.1.0.0\n\n* First version.\n"
basic-deps:
  bytestring: (>=0.9.2.1)
  base: (>=4.5 && <5.0)
  pipes-bytestring: (>=2.0 && <2.2)
  pipes-parse: (>=3.0.1 && <3.1)
  pipes: (>=4.1 && <4.2)
  pipes-attoparsec: (>=0.5 && <0.6)
  attoparsec: (>=0.10 && <0.14)
  transformers: (>=0.2 && <0.5)
  aeson: (>=0.6.1 && <0.9)
all-versions:
- '0.1.0.0'
- '0.2.0'
- '0.2.1'
- '0.3.0'
- '0.4'
- '0.4.1'
- '0.4.1.1'
- '0.4.1.2'
- '0.4.1.3'
author: Renzo Carbonara
latest: '0.4.1.3'
description-type: markdown
description: ! '# pipes-aeson


  Utilities to encode and decode streams of **JSON** values using **Pipes** and

  **Aeson**.


  Check the source or rendered Haddocks for documentation.


  This code is licensed under the terms of the so called **3-clause BSD

  license**. Read the file named ``LICENSE`` found in this same directory

  for details.


  See the ``PEOPLE`` file to learn about the people involved in this

  effort.

'
license-name: BSD3
