homepage: https://github.com/kadena-io/streaming-events
changelog-type: markdown
hash: 5ef09f0010204865ca359ed4b7803b04756a649dc56e7f61a881c4661927f234
test-bench-deps: {}
maintainer: colin@kadena.io
synopsis: Client-side consumption of a ServerEvent.
changelog: |
  # Changelog for `streaming-events`

  ## 1.0.0

  - Initial release.
basic-deps:
  http-client: ! '>=0.5 && <0.7'
  streaming: ^>=0.2
  bytestring: ^>=0.10
  base: ! '>=4.9 && <5'
  binary: ^>=0.8
  attoparsec: ^>=0.13
  wai-extra: ^>=3.0
  streaming-bytestring: ^>=0.1
  streaming-attoparsec: ^>=1.0
all-versions:
- 1.0.0
author: Colin Woodbury
latest: 1.0.0
description-type: markdown
description: |
  # streaming-events

  Client-side consumption of the `ServerEvent` type from `wai-extra` via the
  `streaming` ecosystem.
license-name: BSD-3-Clause
