all-versions:
- '0.1'
- 0.1.0.1
- '0.2'
author: Renzo Carbonara
basic-deps:
  aeson: '>=0.8 && <0.10'
  base: '>=4.8 && <4.9'
  instant-generics: '>=0.4 && <0.6'
changelog: |
  # Version 0.2

  * Rename 'gtoJSON' to 'gtoJSONDefault'.

  * Rename 'gparseJSON' to 'gparseJSONDefault'.

  * Export 'RepGToJSON'.

  * Export 'RepGFromJSON'.


  # Version 0.1.0.1

  * Raise `instant-generics` dependency upper bound.


  # Version 0.1

  * First release
changelog-type: markdown
description: |
  `instant-aeson` allows you to generically derive JSON representations (from the
  `aeson` package) for your types using `instant-generics`, which in particular,
  supports deriving generic representations for GADTs.
description-type: markdown
hash: b1ae3eea51e2e9bd660c586f7868617fd1b54ebc145309286d5aea4ac9384778
homepage: https://github.com/k0001/instant-aeson
latest: '0.2'
license-name: BSD-3-Clause
maintainer: renzo@carbonara.com.ar
synopsis: Generic Aeson instances through instant-generics
test-bench-deps:
  aeson: '>=0'
  base: '>=4 && <5'
  instant-aeson: '>=0'
  instant-generics: '>=0'
  tasty: '>=0.10'
  tasty-quickcheck: '>=0.8'
