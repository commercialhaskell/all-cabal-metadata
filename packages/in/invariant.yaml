homepage: https://github.com/nfrisby/invariant-functors
changelog-type: markdown
hash: de32ece71e598822a610d7f0b0161195d16af9cb7450280990cfeaf4a351db18
test-bench-deps:
  base: ! '>=4 && <5'
  hspec: ! '>=1.8'
  invariant: -any
  QuickCheck: ! '>=2 && <3'
maintainer: ! 'Nicolas Frisby <nicolas.frisby@gmail.com>,

  Ryan Scott <ryan.gl.scott@ku.edu>'
synopsis: Haskell 98 invariant functors
changelog: ! "# 0.2\n* Support deriving `Invariant` and `Invariant2` instances with
  Template Haskell\n* Added `invmapFunctor`, `invmapContravariant`, `invmap2Bifunctor`,
  and\n  `invmap2Profunctor` to make defining `Invmap` and `Invmap2` instances\n  somewhat
  easier\n* Added `WrappedFunctor`, `WrappedContravariant`, `WrappedBifunctor`, and\n
  \ `WrappedProfunctor` data types to allow use of `invmap` and `invmap2` for\n  data
  types that aren't `Invariant` or `Invariant2` instances.\n* Added `Invariant` instances
  for lazy `ST`, `ArrowMonad`, `Handler`,\n  `Identity`, `First`, `Last`, `Alt`, `Proxy`,
  `ArgDescr`, `ArgOrder`, and\n  `OptDescr`\n* Added `Invariant` and `Invariant2`
  instances for data types in the `array`,\n  `bifunctors`, `containers`, `profunctors`,
  `semigroups`, `stm`, `tagged`,\n  `transformers`, and `unordered-containers` libraries\n\n#
  0.1.2\n* Add `Invariant` instances for `Dual` and `Endo`\n\n# 0.1.1\n* Bump `contravariant`
  upper version bounds\n\n# 0.1.0\n* Initial commit\n"
basic-deps:
  stm: ! '>=2.2 && <3'
  base: ! '>=4 && <5'
  unordered-containers: ! '>=0.2.4 && <0.3'
  semigroups: ! '>=0.16.2 && <1'
  array: ! '>=0.3 && <0.6'
  tagged: ! '>=0.7.3 && <1'
  containers: ! '>=0.1 && <0.6'
  bifunctors: ! '>=5 && <6'
  ghc-prim: -any
  contravariant: ! '>=0.1.2 && <2'
  transformers: ! '>=0.2 && <0.5'
  template-haskell: ! '>=2.4 && <2.11'
  transformers-compat: ! '>=0.3 && <1'
  profunctors: ! '>=5 && <6'
all-versions:
- '0.1.0'
- '0.1.1'
- '0.1.2'
- '0.2'
author: Nicolas Frisby <nicolas.frisby@gmail.com>
latest: '0.2'
description-type: markdown
description: ! '# `invariant` [![Hackage version](https://img.shields.io/hackage/v/invariant.svg?style=flat)](http://hackage.haskell.org/package/invariant)
  [![Build Status](https://img.shields.io/travis/nfrisby/invariant-functors.svg?style=flat)](https://travis-ci.org/nfrisby/invariant-functors)


  Haskell98 invariant functors

'
license-name: BSD3
