all-versions:
- 0.1.0.0
- 0.1.0.1
author: Herbert Valerio Riedel
basic-deps:
  array: ^>=0.5.1.0
  base: '>=4.8.0.0 && <5'
  bytestring: '>=0.10.6.0 && <0.13'
  containers: ^>=0.5.6.2 || ^>=0.6.0.1 || ^>=0.7
  deepseq: ^>=1.4.1.1 || ^>=1.5.0.0
  fail: ^>=4.9.0.0
  text: ^>=1.2.2.2 || ^>=2.0 || ^>=2.1
changelog: |
  ### 0.1.0.1

  _Andreas Abel, 2022-05-26_

  - Allow `base >= 4.17`
  - Tested with GHC 7.10 - 9.4
changelog-type: markdown
description: |
  # microaeson: A tiny JSON library with light dependency footprint

  `microaeson` aims to provide a [RFC 8259](https://tools.ietf.org/html/rfc8259)
  compliant JavaScript Object Notation (JSON) implementation.

  The [microaeson](https://hackage.haskell.org/package/microaeson) package provides
  a smaller subset of the [aeson](https://hackage.haskell.org/package/aeson) API
  with less dependencies and a simpler implementation.

  The API is designed in such a way to allow to easily convert client
  code written against `microaeson`'s API to use the full
  [aeson](https://hackage.haskell.org/package/aeson) API.

  (C) 2016-2018 Herbert Valerio Riedel

  License: GPL-3
description-type: markdown
hash: 8bf2cf17917708f6cee442f22af57671b00d96697f68caeafc2383ba93a937f1
homepage: ''
latest: 0.1.0.1
license-name: GPL-3.0-only
maintainer: Andreas Abel
synopsis: A tiny JSON library with light dependency footprint
test-bench-deps:
  QuickCheck: ^>=2.11.3 || ^>=2.12.6.1 || ^>=2.13.2 || ^>=2.14.2
  aeson: ^>=1.3.1.0 || ^>=1.4.7.1 || ^>=1.5.6.0 || ^>=2.0.3.0 || ^>=2.1.0.0 || ^>=2.2.0.0
  base: '>=0'
  bytestring: '>=0'
  containers: '>=0'
  microaeson: '>=0'
  quickcheck-instances: ^>=0.3.16
  tasty: '>=1.0.1.1 && <1.6'
  tasty-quickcheck: ^>=0.10
  text: '>=0'
  unordered-containers: ^>=0.2.8.0
  vector: ^>=0.12.0.1 || ^>=0.13.0.0
