all-versions:
- 0.1.0.0
- 0.1.1.0
author: Lucas DiCioccio, Alejandro Serrano
basic-deps:
  base: '>=4.10 && <5'
  binary: '>=0.8.5 && <0.11'
  bytestring: '>=0.10.8 && <0.12'
  case-insensitive: '>=1.2.0 && <1.3'
  http2-grpc-types: '>=0'
  proto-lens: '>=0.5 && <0.8'
  zlib: '>=0.6.2 && <0.7'
changelog: |
  # Changelog for http2-grpc-proto-lens

  ## Unreleased changes
changelog-type: markdown
description: |
  # http2-grpc-types

  A module to put in common HTTP2/gRPC types, values, and helpers.
description-type: markdown
hash: a41fcee365a4f6a8f9f839ac39b1d7c3239911ab1ec99047465813eb2ca6bca5
homepage: https://github.com/haskell-grpc-native/http2-grpc-haskell#readme
latest: 0.1.1.0
license-name: BSD-3-Clause
maintainer: lucas@dicioccio.fr
synopsis: Encoders based on `proto-lens` for gRPC over HTTP2.
test-bench-deps: {}
