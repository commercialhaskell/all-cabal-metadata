homepage: https://hslua.org/
changelog-type: markdown
hash: d8f9f892641da57ac86efe4852200bb3f18c4e1cbedbb4410041a96eaf75dd6f
test-bench-deps:
  hslua-list: -any
  base: '>=4.9.1 && <5'
  hslua-core: '>=2.3 && <2.4'
  tasty: '>=0.11'
  tasty-lua: '>=1.0'
maintainer: Albert Krewinkel <albert@hslua.org>
synopsis: Opinionated, but extensible Lua list type.
changelog: |
  # Changelog

  `hslua-list` uses [PVP Versioning](https://pvp.haskell.org).

  ## hslua-list-1.0.0

  Released 2022-10-10.

  -   To boldly go where no Haskell library has gone before.
basic-deps:
  bytestring: -any
  hslua-marshalling: '>=2.1 && <2.3'
  base: '>=4.9.1 && <5'
  hslua-core: '>=2.3 && <2.4'
all-versions:
- 1.0.0
author: Albert Krewinkel
latest: 1.0.0
description-type: markdown
description: |
  hslua-list
  ==========

  Opinionated, fast, and extensible implementation of a Lua *List*
  type. The type re-uses some parts of Lua's base library, so the
  *base* library must be loaded before this one.
license-name: MIT
