all-versions:
- 0.0.1.0
- 0.1.0.0
- 0.1.0.1
- 0.1.0.2
- 1.0.0.0
author: Ian Duncan, Jade Lovelace
basic-deps:
  base: '>=4.7 && <5'
  hs-opentelemetry-api: '>=1.0 && <1.1'
  hs-opentelemetry-semantic-conventions: '>=1.40 && <2'
  mtl: '>=0'
  persistent: '>=2.13.3'
  resourcet: '>=0'
  text: '>=0'
  unliftio: '>=0'
  unordered-containers: '>=0'
  vault: '>=0'
changelog: |
  # Changelog for hs-opentelemetry-persistent

  ## Unreleased

  ## 1.0.0.0 - 2026-05-29

  - Internal: `db.transaction.*` attribute keys are now typed `AttributeKey` constants
    (`db.transaction.isolation`, `db.transaction.outcome`,
    `db.transaction.commit_duration_us`, `db.transaction.rollback_duration_us`).
    Attribute names are unchanged.

  - **Respect `OTEL_SEMCONV_STABILITY_OPT_IN` for database query attribute.**
    Query text is now emitted as `db.query.text` (stable), `db.statement` (old),
    or both (`database/dup`), controlled by the `databaseOption` setting.
  - **Fix: span name now low-cardinality.** Previously used the full SQL string as
    the span name, which is high cardinality and violates OTel naming rules. Now uses
    `{db.operation.name} {db.namespace}` pattern (e.g. `SELECT mydb`). Full SQL text
    remains available in the `db.query.text` / `db.statement` attributes.
  - Add `db.operation.name` attribute in stable mode (extracted from first SQL keyword).
  - `annotateBasics` emits `db.system.name` (stable) or `db.system` (old) per config.

  ## 0.1.0.2

  - Relax `hs-opentelemetry-api` bounds to support 0.3.x

  ## 0.1.0.1

  - Support newer dependencies

  ## 0.1.0.0

  ### Breaking changes

  - Use `HashMap Text Attribute` instead of `[(Text, Attribute)]` as attributes

  ## 0.0.1.0

  - Initial release
changelog-type: markdown
description: |
  # hs-opentelemetry-instrumentation-persistent

  [![hs-opentelemetry-instrumentation-persistent](https://img.shields.io/hackage/v/hs-opentelemetry-instrumentation-persistent?style=flat-square&logo=haskell&label=hs-opentelemetry-instrumentation-persistent&labelColor=5D4F85)](https://hackage.haskell.org/package/hs-opentelemetry-instrumentation-persistent)
description-type: markdown
hash: 36ca683627044596c1cd3e0863d9c735c3e3dd017110fdb40d59003c1359b744
homepage: https://github.com/iand675/hs-opentelemetry#readme
latest: 1.0.0.0
license-name: BSD-3-Clause
maintainer: ian@iankduncan.com
synopsis: OpenTelemetry instrumentation for the Persistent database library
test-bench-deps:
  base: '>=4.7 && <5'
  hs-opentelemetry-api: '>=1.0 && <1.1'
  hs-opentelemetry-instrumentation-persistent: '>=1.0 && <1.1'
  hs-opentelemetry-semantic-conventions: '>=1.40 && <2'
  hspec: '>=0'
  text: '>=0'
  unordered-containers: '>=0'
