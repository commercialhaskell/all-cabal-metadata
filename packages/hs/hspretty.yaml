homepage: ''
changelog-type: markdown
hash: 1a9bf3e38d77cd2afe9a8fbdc8b14a7d6a39877022c707cfcdd9234e6cf787af
test-bench-deps:
  base: ^>=4.14.1.0 || ^>=4.15.0.0
  doctest: ^>=0.18.1
maintainer: j.s.merritt@gmail.com
synopsis: My opinionated Haskell project formatter.
changelog: |
  # Revision history for hspretty

  ## 0.2.0.0 -- 2021-08-21

  - GHC 9.0.1 support.
  - Bumped `streamly` to `0.8.0` (some API changes).
  - Bumped package bounds.

  ## 0.1.0.0 -- 2021-05-16

  - First version. Created as a Haskell package from this Gist:
    https://gist.github.com/lancelet/9a9d5e207d36be20e0e1f16fbe51bb7d
basic-deps:
  void: ^>=0.7.3
  bytestring: ^>=0.10.12.0
  text-short: ^>=0.1.3
  unliftio: ^>=0.2.16
  path: ^>=0.8.0 || ^>=0.9.0
  base: '>=4.14.1.0 && <4.15 || >=4.15.0.0 && <4.16'
  hspretty: -any
  text: ^>=1.2.4.1
  process: ^>=1.6.9.0
  ansi-terminal: ^>=0.11
  optparse-applicative: ^>=0.16.1.0
  ormolu: ^>=0.1.4.1 || ^>=0.2.0.0
  path-io: ^>=1.6.2
  streamly: ^>=0.8.0
  directory: ^>=1.3.6.0
all-versions:
- 0.1.0.0
- 0.2.0.0
author: Jonathan Merritt
latest: 0.2.0.0
description-type: haddock
description: |-
  This is a formatter for Haskell projects (for example, for *.cabal files and
  *.hs files) that can perform both in-place formatting and formatting checks.
  In the background, it uses Ormolu and cabal-fmt. Please make sure that
  cabal-fmt is installed separately before running it, since it invokes
  cabal-fmt as a command-line utility.
  .
  It is licensed under the BSD-3-Clause license to match Ormolu and cabal-fmt.
license-name: BSD-3-Clause
