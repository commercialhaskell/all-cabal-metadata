homepage: https://github.com/hvr/HsYAML
changelog-type: ''
hash: e2bf69c1e71996031ca40f4c3ae86b430f702b50626010007b8fd6914a711076
test-bench-deps: {}
maintainer: hvr@gnu.org
synopsis: Pure Haskell YAML 1.2 parser
changelog: ''
basic-deps:
  bytestring: ! '>=0.9 && <0.11'
  base: ! '>=4.5 && <4.12'
  text: ! '>=1.2.3 && <1.3'
  parsec: ! '>=3.1.13.0 && <3.2'
  dlist: ! '>=0.8 && <0.9'
  containers: ! '>=0.4.2 && <0.6'
  mtl: ! '>=2.2.1 && <2.3'
all-versions:
- '0.1.0.0'
author: Herbert Valerio Riedel
latest: '0.1.0.0'
description-type: haddock
description: ! '@HsYAML@ is a [YAML 1.2](http://yaml.org/spec/1.2/spec.html) parser
  for Haskell.


  Features include:


  * Pure Haskell implementation with small dependency footprint and emphasis on strict
  compliance with the [YAML 1.2 specification](http://yaml.org/spec/1.2/spec.html)

  * Low-level API access to lexical token-based scanner ("Data.YAML.Token")

  * Event-based API resembling LibYAML''s Event-based API ("Data.YAML.Event")

  * Support for constructing custom YAML node graph representation (including support
  for cyclic YAML data structures)

  * Direct decoding via typeclass-based API similiar to @aeson@ ("Data.YAML")

  * Support for /Failsafe/, /JSON/ and /Core/ schemas as defined in the YAML 1.2 specification
  (including support for user-defined custom schemas)'
license-name: GPL-3
