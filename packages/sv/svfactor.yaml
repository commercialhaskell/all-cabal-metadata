homepage: https://github.com/qfpl/svfactor
changelog-type: markdown
hash: 4dea4b2564147911c5467ccb63b0efccd262759f4aa126896295d0918616e3c0
test-bench-deps:
  bytestring: ! '>=0.9.1.10 && <0.11'
  trifecta: ! '>=1.5 && <2.1'
  base: ! '>=4.7 && <5'
  tasty-hedgehog: ! '>=0.1 && <0.3'
  text: ! '>=1.0 && <1.3'
  svfactor: -any
  hedgehog: ! '>=0.5 && <0.7'
  semigroups: ! '>=0.18 && <0.19'
  parsers: ! '>=0.12 && <0.13'
  lens: ! '>=4 && <5'
  utf8-string: ! '>=1 && <1.1'
  tasty-hunit: ! '>=0.9 && <0.11'
  tasty: ! '>=0.11 && <1.2'
  vector: ! '>=0.10 && <0.13'
maintainer: george@qfpl.io
synopsis: Syntax-preserving CSV manipulation
changelog: ! '# Revision history for svfactor


  ## 0.1 -- 2018-07-19


  * First version. Released on an unsuspecting world.

'
basic-deps:
  semigroupoids: ! '>=5 && <6'
  bytestring: ! '>=0.9.1.10 && <0.11'
  trifecta: ! '>=1.5 && <2.1'
  base: ! '>=4.7 && <5'
  text: ! '>=1.0 && <1.3'
  parsec: ! '>=3.1 && <3.2'
  semigroups: ! '>=0.18 && <0.19'
  parsers: ! '>=0.12 && <0.13'
  lens: ! '>=4 && <5'
  bifunctors: ! '>=5.1 && <6'
  utf8-string: ! '>=1 && <1.1'
  charset: ! '>=0.3 && <=0.4'
  attoparsec: ! '>=0.12.1.4 && <0.14'
  transformers: ! '>=0.2 && <0.6'
  deepseq: ! '>=1.1 && <1.5'
  vector: ! '>=0.10 && <0.13'
all-versions:
- '0.1'
author: George Wilson
latest: '0.1'
description-type: haddock
description: ! '<<http://i.imgur.com/uZnp9ke.png>>


  svfactor is a library for parsing, manipulating, and printing CSV and

  similar formats (such as PSV, TSV, and many more).


  svfactor retains all syntactic information including newlines, and

  provides lenses, prisms, and traversals to query and manipulate this

  structure.

  This should make it useful for writing custom CSV transformations and

  sanitisation tools. For example, one could easily use it to rewrite all

  CRLF newlines to LF.

  It also extends RFC4180 by allowing optional spacing surrounding fields.


  svfactor''s parser is exposed so you can use it independently and printing is similarly
  standalone.


  Please note that there are __known performance problems with svfactor__.


  Examples:


  * Handling multiple logical documents in one file: <https://github.com/qfpl/svfactor/blob/master/examples/src/Data/Svfactor/Example/Concat.hs
  Concat.hs>

  * Fixing inconsistent formatting with lenses: <https://github.com/qfpl/svfactor/blob/master/examples/src/Data/Svfactor/Example/Requote.hs
  Requote.hs>'
license-name: BSD3
