all-versions:
- '0.1'
- 0.1.1
author: Renzo Carbonara
basic-deps:
  async: '>=0'
  base: '>=4 && <5'
  containers: '>=0'
  hashable: '>=0'
  mmzk-typeid: '>=0'
  resourcet: '>=0'
  resourcet-extra: '>=0'
  safe-exceptions: '>=0'
  stm: '>=0'
  time: '>=0'
  transformers: '>=0'
changelog: |
  # Version 0.1.1

  * Fixed CHANGELOG file


  # Version 0.1

  * Initial version.
changelog-type: markdown
description: |+
  # job

  Job queue thing in Haskell.

  * Copyright © Renzo Carbonara, 2024.

  * Licensing terms in `LICENSE` file (`Apache-2.0`).



  ## Development

  1. Clone this repo and run `nix develop` in the top-level folder.

  2. Within that environment, you can use `cabal` as usual.

  3. If you don't have `nix` installed, then have `nix` installed.

description-type: markdown
hash: 8f94850360ce09f8079c58257a14a3c2f80bf76326cc70a063436456042ecf40
homepage: https://github.com/k0001/hs-job
latest: 0.1.1
license-name: Apache-2.0
maintainer: renλren.zone
synopsis: Job queue
test-bench-deps:
  base: '>=4 && <5'
  job: '>=0'
  random: '>=0'
  resourcet: '>=0'
  time: '>=0'
