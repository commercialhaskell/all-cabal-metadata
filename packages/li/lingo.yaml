homepage: https://github.com/tclem/lingo-haskell
changelog-type: markdown
hash: aca2f51328443ab0ebc7362da75bd89e395912324d134063a18076c6ce298083
test-bench-deps:
  base: -any
  hspec: ^>=2.7.1
  lingo: -any
maintainer: timothy.clem@gmail.com
synopsis: File extension based programming language detection
changelog: |
  # 0.3.0.0

  `languageForPath` is now `languagesForPath`, which returns multiple results in cases of ambiguity.

  # 0.2.0.0

  Fix bug where `Gemfile.lock` files were being recognized as Ruby.

  # 0.1.0.1

  Adds documentation.

  # 0.1.0.0

  Initial release.
basic-deps:
  bytestring: ^>=0.10.8.2
  base: ! '>=4.12 && <5'
  text: ^>=1.2.3.1
  filepath: ^>=1.4.2.1
  containers: ^>=0.6.0.1
  raw-strings-qq: ^>=1.1
  yaml: ^>=0.11.1.2
all-versions:
- 0.1.0.1
- 0.2.0.0
- 0.3.0.0
author: Timothy Clem
latest: 0.3.0.0
description-type: markdown
description: |
  # Lingo

  Detect programming languages from file extensions and common filenames. Based on [linguist's](https://github.com/github/linguist) registry of languages.

  ## Development

  ```
  cabal new-update
  cabal new-configure
  cabal new-build
  ```
license-name: BSD-3-Clause
