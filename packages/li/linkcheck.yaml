all-versions:
- 0.1.0.0
- 0.2.0.0
- 0.3.0.0
author: Tom Sydney Kerckhove
basic-deps:
  aeson: '>=0'
  base: '>=4.7 && <5'
  bytestring: '>=0'
  conduit: '>=0'
  containers: '>=0'
  http-client: '>=0'
  http-client-tls: '>=0'
  http-types: '>=0'
  linkcheck: '>=0'
  list-t: '>=0'
  lrucache: '>=0'
  monad-logger: '>=0'
  mtl: '>=0'
  network-uri: '>=0'
  opt-env-conf: '>=0'
  path: '>=0'
  path-io: '>=0'
  retry: '>=0'
  stm: '>=0'
  stm-containers: '>=0'
  tagsoup: '>=0'
  text: '>=0'
  unliftio: '>=0'
  validity-network-uri: '>=0'
changelog: |+
  # Changelog

  ## [0.3.0.0] - 2026-03-08

  ### Added

  * Check URLs in `<script>`, `<iframe>`, `<video>`, `<audio>`, `<source>`, `<embed>`, and `<object>` tags.
  * Check `<video poster>` attribute for broken URLs.
  * Check `<form action>` attribute for broken URLs.
  * Parse `srcset` attributes on `<img>` and `<source>` tags for broken URLs.

  ## [0.2.0.0] - 2026-03-08

  ### Added

  * Check `<meta>` tags with `property="og:image"` or `name="twitter:image"` for broken image URLs.

  ## [0.1.0.0] - 2022-05-09

  ### Added

  * Linkcheck now caches requests when fragment checking is turned on.

changelog-type: markdown
description: ''
description-type: haddock
hash: 315bc3298583de64c9aadb0ba1b69eb7bd1dda88b588113b5d15284490e258fd
homepage: https://github.com/NorfairKing/linkcheck#readme
latest: 0.3.0.0
license-name: MIT
maintainer: syd@cs-syd.eu
synopsis: Check for broken links in CI
test-bench-deps: {}
...
