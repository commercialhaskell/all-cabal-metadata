all-versions:
- 0.1.0.0
- '0.2'
- 0.2.1
- 0.2.2
- '0.3'
- '0.4'
author: Iavor Diatchki
basic-deps:
  HUnit: '>=1.3.0 && <1.7'
  base: '>=4.9 && <4.19'
  containers: '>=0.5 && <0.7'
  directory: '>=1.3 && <1.4'
  filepath: '>=1.4 && <1.5'
  process: '>=1.4 && <1.7'
  simple-get-opt: '>=0.2.0 && <0.5'
  test-framework: '>=0.8.0 && <0.9'
  test-framework-hunit: '>=0.3.0 && <0.4'
  test-lib: '>=0'
changelog: |
  # Revision history for test-lib

  ## 0.3.1

  * Make it work with GHC 9

  ## 0.3

  * Add --version flag to report current version
  * Add OS specific gold files.  If there is a file named TEST.stdout.OS
    where OS matches the current OS, then this file will be used instead of
    the TEST.stdout.   The names of the OS are:
      darwin            (for Mac)
      freebsd
      linux
      linux-android
      mingw32           (for Windows)
      netbsd
      openbsd

  ## 0.2.1 -- 2018-01-21

  * Only changes to versions of package dependencies.

  ## 0.1.0.0 -- 2018-12-11

  * First version. Released on an unsuspecting world.
changelog-type: markdown
description: |-
  This library makes it easy to define an executable,
  which can find and run a bunch of tests for a binary.
description-type: haddock
hash: 8ec9bda243ece02b95b56dd56ce4f894645f2b6f91b89caf2e76d7ec835bec0d
homepage: ''
latest: '0.4'
license-name: ISC
maintainer: iavor.diatchki@gmail.com
synopsis: A library to make a quick test-runner script.
test-bench-deps: {}
