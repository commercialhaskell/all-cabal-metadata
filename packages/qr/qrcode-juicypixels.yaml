homepage: https://github.com/alexkazik/qrcode#readme
changelog-type: markdown
hash: 34514646c37f67f0d0f285ee0f4dcc84e3f29bba1628072f9b0ebe97b1ef76af
test-bench-deps: {}
maintainer: alex@kazik.de
synopsis: Converts a qrcode-core image to JuicyPixels
changelog: |
  # Changelog for qrcode-juicypixels

  ## 0.8.0 -- 2019-01-xx

  * Initial release
basic-deps:
  JuicyPixels: ! '>=3.2.6 && <3.4'
  bytestring: ! '>=0.10.6.0 && <0.11'
  base: ! '>=4.8.2.0 && <5'
  base64-bytestring: ! '>=1.0.0.1 && <1.1'
  text: ! '>=1.2.2.0 && <1.3'
  qrcode-core: ! '>=0.8 && <1'
  vector: ! '>=0.11.0.0 && <0.13'
all-versions:
- 0.8.0
author: ALeX Kazik
latest: 0.8.0
description-type: markdown
description: |
  # qrcode

  QR code library in pure Haskell

  ## qrcode-juicypixels

  This package adds a function to convert the QRCode into a JuicyPixels image.
  And some to create a data url containing the QRCode as a PNG.
license-name: MIT
