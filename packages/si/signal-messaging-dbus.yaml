all-versions:
- 1.0.0.2
- 1.0.1.0
author: Lia Lenckowski
basic-deps:
  base: '>=4.7 && <5'
  bytestring: <0.12
  dbus: '>=1.2.12 && <1.3'
  signal-messaging-dbus: '>=0'
  time: '>=1.8 && <1.13'
  unliftio: '>=0.2 && <0.3'
changelog: |
  # 1.0.1.0
  - add Timestamp conversion from/to UTCTime
  - add replyTo, reactTo

  # 1.0.0.2
  - add version bounds on deps
  - add changelog

  # 1.0.0.1
  - add more package metadata

  # 1.0.0.0
  - initial stable release
changelog-type: markdown
description: "# signal-messaging-dbus\nLibrary for connecting a haskell program to
  a [signal-cli](https://github.com/AsamK/signal-cli)-instance running in daemon mode,
  using its DBus interface.\n\nSupport exists for almost the whole api, which should
  be sufficient for your bot-writing needs.\n\nThe library is fully documented with
  haddock, and mostly tested.\nYou can either build the documentation yourself with
  `stack haddock --open signal-messaging-dbus`, or look at it on hackage.  \nFor an
  a small code example, see `example/Main.hs`.\n"
description-type: markdown
hash: 4e1d281ac47c8389b8a01143893ae3e161e970c99e29162771f0eeb3ebd197c1
homepage: https://codeberg.org/lialenck/signal-messaging-dbus
latest: 1.0.1.0
license-name: LicenseRef-AGPL
maintainer: lialenck@protonmail.com
synopsis: Bindings for signal-cli's DBus interface
test-bench-deps: {}
