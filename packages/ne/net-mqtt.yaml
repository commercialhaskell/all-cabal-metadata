homepage: https://github.com/dustin/mqtt#readme
changelog-type: ''
hash: 32110b3e931f1ccddc7cb32c5d18d8eeff8e16b4feebcb3fefafa48e84eb6da0
test-bench-deps:
  bytestring: ! '>=0.10.8 && <0.11'
  stm: ! '>=2.5.0 && <2.6'
  base: ! '>=4.7 && <5'
  text: ! '>=1.2.3 && <1.3'
  network-conduit-tls: ! '>=1.3.2 && <1.4'
  async: ! '>=2.2.1 && <2.3'
  conduit: ! '>=1.3.1 && <1.4'
  HUnit: -any
  conduit-extra: ! '>=1.3.0 && <1.4'
  containers: ! '>=0.6.0 && <0.7'
  binary: ! '>=0.8.6 && <0.9'
  tasty-quickcheck: -any
  tasty-hunit: -any
  attoparsec: ! '>=0.13.2 && <0.14'
  tasty: -any
  QuickCheck: -any
  net-mqtt: -any
maintainer: dustin@spy.net
synopsis: An MQTT Protocol Implementation.
changelog: ''
basic-deps:
  bytestring: ! '>=0.10.8 && <0.11'
  stm: ! '>=2.5.0 && <2.6'
  base: ! '>=4.7 && <5'
  text: ! '>=1.2.3 && <1.3'
  network-conduit-tls: ! '>=1.3.2 && <1.4'
  async: ! '>=2.2.1 && <2.3'
  conduit: ! '>=1.3.1 && <1.4'
  conduit-extra: ! '>=1.3.0 && <1.4'
  containers: ! '>=0.6.0 && <0.7'
  binary: ! '>=0.8.6 && <0.9'
  attoparsec: ! '>=0.13.2 && <0.14'
  net-mqtt: -any
all-versions:
- 0.1.0.0
author: Dustin Sallings
latest: 0.1.0.0
description-type: markdown
description: |
  # mqtt

  An [MQTT][mqtt] protocol implementation for Haskell.

  ## Client Examples

  ### Publish

  ```haskell
  main :: IO
  main = do
    mc <- runClient mqttConfig{}
    publish mc "tmp/topic" "hello!" False
  ```

  ### Subscribe

  ```haskell
  main :: IO
  main = do
    mc <- runClient mqttConfig{_msgCB=Just msgReceived}
    print =<< subscribe mc [("tmp/topic1", QoS0), ("tmp/topic2", QoS0)]
    print =<< waitForClient mc   -- wait for the the client to disconnect

    where
      msgReceived t m = print (t,m)
  ```

  [mqtt]: http://mqtt.org/
license-name: BSD-3-Clause
