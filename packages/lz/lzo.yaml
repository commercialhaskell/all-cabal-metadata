homepage: ''
changelog-type: markdown
hash: cb6ca4b2f99d8820f668d805a99418e0a308104413b299ffe57b9391c3a1f4fc
test-bench-deps:
  bytestring: -any
  base: -any
  criterion: -any
  tasty-hunit: -any
  tasty: -any
  lzo: -any
maintainer: vamchale@gmail.com
synopsis: minilzo bundled for Haskell
changelog: |
  # lzo

  ## 0.1.1.4

    * Add `pure` flag to depend on digest-pure if desired

  ## 0.1.1.3

    * Add `cross` flag for cross-compilation
    * Use unsafety to make it faster in various places

  ## 0.1.1.2

    * Set `cc-options: -O3`
    * Set `ghc-options: -O2`

  ## 0.1.1.1

    * Use [digest](http://hackage.haskell.org/package/digest) over
      [digest-pure](http://hackage.haskell.org/package/digest-pure).

  ## 0.1.1.0

    * Added `lzoVersionString` etc.
    * Set file mode bit more sensibly

  ## 0.1.0.0

  Initial release
basic-deps:
  bytestring: -any
  base: '>=4.10 && <5'
  binary: '>=0.8.3.0'
  digest: -any
  digest-sig: -any
all-versions:
- 0.1.1.0
- 0.1.1.1
- 0.1.1.2
- 0.1.1.3
- 0.1.1.4
author: Vanessa McHale
latest: 0.1.1.4
description-type: markdown
description: |
  # lzo

  This bundles [minilzo](http://www.oberhumer.com/opensource/lzo/#minilzo) and
  hence only offers one compression level.

  It contains a file (frame) compressor as well as block compression. File
  compression (and decompression) is fully lazy.
license-name: BSD-3-Clause
