homepage: ''
changelog-type: markdown
hash: 88e2f0830c81d76353ecee3234d81c88ddc487e3c84b8898b0a47d750a0f9c1a
test-bench-deps:
  bytestring: -any
  base: -any
  filepath: -any
  criterion: -any
  lz4-hs: -any
  tasty-hunit: -any
  temporary: -any
  tasty: -any
maintainer: vamchale@gmail.com
synopsis: lz4 bindings for Haskell
changelog: |
  # lz4

  ## 0.1.2.0

    * Add `cross` flag
    * Fix some stuff for cross-compiling

  ## 0.1.1.0

    * Export `lZ4VersionString` and `lZ4VersionNumber`
    * Add `lZ4MaxInputSize` and `lZ4HCClevelMax`
    * Add `compressBlockHC`
    * Add `{-# NOINLINE -#}` pragmas in various places

  ## 0.1.0.0

  Initial release
basic-deps:
  bytestring: -any
  base: ! '>=4.8 && <5'
all-versions:
- 0.1.0.0
- 0.1.1.0
- 0.1.2.0
author: Vanessa McHale
latest: 0.1.2.0
description-type: markdown
description: |
  # lz4-hs

  Haskell bindings to [lz4](https://github.com/lz4/lz4). Includes a bundled copy
  of lz4 1.9.2

  ## Comparison

  lz4-hs includes support for frames as well as blocks, unlike
  [lz4](https://hackage.haskell.org/package/lz4) and
  [lz4-conduit](https://hackage.haskell.org/package/lz4-conduit).

  This means you can work with `.lz4` files.
license-name: BSD-3-Clause
