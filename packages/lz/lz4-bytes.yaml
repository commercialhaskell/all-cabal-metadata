homepage: https://github.com/byteverse/lz4-bytes
changelog-type: markdown
hash: 2ad7c888af82188bf2d37bdc064f2bce996e6b0cc50d5de0eb6ee6a8304234a4
test-bench-deps:
  base: ! '>=4.11.1 && <5'
  lz4-bytes: -any
  byteslice: -any
  tasty-quickcheck: -any
  tasty: -any
  primitive: -any
maintainer: andrew.thaddeus@gmail.com
synopsis: Bindings to LZ4
changelog: |
  # Revision history for lz4-bytes

  ## 0.1.0.2 -- 2020-03-09

  * Add `lz4hc.h` to `extra-source-files`.

  ## 0.1.0.1 -- 2020-03-09

  * Add `lz4.h` to `extra-source-files`.

  ## 0.1.0.0 -- 2020-01-31

  * First version. Released on an unsuspecting world.
basic-deps:
  base: ! '>=4.12 && <5'
  run-st: ! '>=0.1.1 && <0.2'
  byteslice: ! '>=0.1.4 && <0.3'
  primitive: ! '>=0.7 && <0.8'
all-versions:
- 0.1.0.0
- 0.1.0.1
- 0.1.0.2
author: Andrew Martin
latest: 0.1.0.2
description-type: haddock
description: |-
  This library is similar to the @lz4@ library except that it works
  with the @Bytes@ type from @byteslice@ rather than @ByteString@.
license-name: BSD-3-Clause
