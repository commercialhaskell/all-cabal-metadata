homepage: https://github.com/bigmac2k/lz4-conduit
changelog-type: ''
hash: 1e962c6038ba2568a3d6b9a53cfdcd64ed326876012c44f1e65ed34e2aa9677d
test-bench-deps:
  bytestring: ! '>=0.10 && <0.11'
  base: ! '>=4.9 && <5'
  bytestring-arbitrary: ! '>=0.1 && <0.2'
  conduit: ! '>=1.2 && <1.4'
  QuickCheck: ! '>=2.8 && <2.12'
  resourcet: ! '>=1.1 && <1.3'
  lz4-conduit: -any
maintainer: sven@sven.cc
synopsis: LZ4 compression for conduits
changelog: ''
basic-deps:
  bytestring: ! '>=0.10 && <0.11'
  base: ! '>=4.9 && <5'
  conduit: ! '>=1.2 && <1.4'
  conduit-extra: ! '>=1.2 && <1.4'
  binary: ! '>=0.8 && <0.10'
  mtl: ! '>=2.2 && <2.3'
  resourcet: ! '>=1.1 && <1.3'
  lz4-conduit: -any
all-versions:
- '0.2'
- '0.3'
author: Sven Mattsen
latest: '0.3'
description-type: markdown
description: ! '# LZ compression for Data.Conduit


  This library uses https://github.com/lz4/lz4.git to implement an lz4 compression
  conduit in stream mode. Note that the resulting format is custom. The package comes
  with an executable that is compatible.

  Note that this library does not support bytestring chunks of more then 2GB, i.e,
  it does not function correctly if a bytestring > 2GB is send through the conduit.


  # Installation


  We provide [cabal] as well as [stack] files.


  ```bash

  $ cabal install lz4-conduit

  ```


  ```bash

  $ stack build

  ```


  [cabal]: http://www.haskell.org/cbaal

  [stack]: https://www.haskellstack.org

'
license-name: BSD3
