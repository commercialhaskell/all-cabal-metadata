all-versions:
- 0.1.0.0
- 0.1.1.0
author: IOG Ledger Team
basic-deps:
  QuickCheck: '>=2.16.0 && <2.18'
  base: '>=4.18 && <5'
  free: '>=5.2 && <5.3'
  mtl: '>=2.3.1 && <2.4'
  quickcheck-transformer: '>=0.3.1 && <0.4'
changelog: |
  # Revision history for antigen

  ## 0.1.0.0 -- YYYY-mm-dd

  * First version. Released on an unsuspecting world.
changelog-type: markdown
description: "AntiGen is a library that helps with generating negative examples from
  a \nQuickCheck generator. The `AntiGen` monad is designed to be similar to the\n`Gen`
  monad, so that migrating the generators would be as frictionless as \npossible."
description-type: haddock
hash: ba906a964aba577f1b84bc89fb11b8de033d143079886888f787db276770bbbf
homepage: ''
latest: 0.1.1.0
license-name: MIT
maintainer: hackage@iohk.io
synopsis: 'Negatable QuickCheck generators '
test-bench-deps:
  QuickCheck: '>=0'
  antigen: '>=0'
  base: '>=4.20.2.0 && <4.21'
  criterion: '>=0'
  hspec: '>=0'
  quickcheck-transformer: '>=0'
