homepage: https://github.com/mpilgrem/google-static-maps#readme
changelog-type: markdown
hash: c3096be9154912e6a027c036a77c771f99602d7813ff648cc5d7048ce3e8fa27
test-bench-deps: {}
maintainer: public@pilgrem.com
synopsis: Bindings to the Google Static Maps API
changelog: ! "# 0.5.0.2\r\n\r\n* Update dependencies for GHC 8.4.3\r\n\r\n# 0.5.0.1\r\n\r\n*
  Update dependencies for GHC 8.2.1 and relax lower bounds\r\n\r\n# 0.5.0.0\r\n\r\n*
  Depend on more recent version of `servant` package and, consequently,\r\n  `servant-client`,
  `aeson` and `cryptonite` packages\r\n\r\n# 0.4.0.0\r\n\r\n* Implement `language`
  and `region` parameters\r\n\r\n* Implement digital signatures\r\n\r\n# 0.3.0.0\r\n\r\n*
  Fix bug in instance of `ToHttpApiData` for `Location`\r\n\r\n* Rename certain constructors
  of `Element` type to avoid name clashes\r\n  (`Geometry` and `Labels`)\r\n\r\n*
  Move functions and types of anticipated common use to module\r\n  `Web.Google.Maps.Common`\r\n\r\n#
  0.2.0.0\r\n\r\n* Implement `signature` and custom marker icons\r\n\r\n* Move `staticmap`
  from the end of the base URL to the start of the API type\r\n\r\n* Modify `Show`
  instances for certain types (`Element`, `Feature`,\r\n  `MarkerColor`, `PathColor`,
  `StdColor`, `Visibility`)\r\n\r\n# 0.1.0.0\r\n\r\n* Launch implementation. Not yet
  implemented: certain optional parameters\r\n  (`language`, `region` and `signature`);
  address locations; non-PNG image\r\n  formats; custom marker icons; and encoded
  polyline paths\r\n"
basic-deps:
  http-client: ! '>=0.1 && <0.6'
  JuicyPixels: ! '>=1.0 && <3.3'
  bytestring: ! '>=0.10 && <0.11'
  base: ! '>=4.7 && <4.12'
  base64-bytestring: ! '>=0.1.1 && <1.1'
  servant-client: ! '>=0.11 && <0.15'
  text: ! '>=0.1 && <1.3'
  double-conversion: ! '>=0.2 && <2.1'
  bytedump: ! '>=0.9 && <1.1'
  servant: ! '>=0.11 && <0.15'
  memory: ! '>=0.1 && <0.15'
  utf8-string: ! '>=0.3 && <1.1'
  cryptonite: ! '>=0.1 && <0.26'
  network-uri: ! '>=2.6 && <2.7'
  servant-JuicyPixels: ! '>=0.3 && <0.4'
  aeson: ! '>=0.7 && <1.5'
all-versions:
- '0.1.0.0'
- '0.2.0.0'
- '0.3.0.0'
- '0.4.0.0'
- '0.5.0.0'
- '0.5.0.1'
- '0.5.0.2'
author: Mike Pilgrem
latest: '0.5.0.2'
description-type: haddock
description: ! 'The <https://developers.google.com/maps/documentation/static-maps/intro
  Google Static Maps API>

  returns a map as an image via an HTTP request. This library

  provides bindings in Haskell to that API.


  NB: The use of the Google Static Maps API services is

  subject to the <https://developers.google.com/maps/terms Google Maps APIs Terms
  of Service>,

  which terms restrict the use of content.


  This package has no connection with Google Inc. or its

  affiliates.'
license-name: BSD3
