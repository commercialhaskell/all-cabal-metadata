all-versions:
- 1.8.0
author: Vincent Hanquez <vincent@snarc.org>
basic-deps:
  base: '>=3 && <5'
  bytestring: '>=0'
  crypton: '>=0'
  crypton-asn1-encoding: '>=0'
  crypton-asn1-types: '>=0.4.1 && <0.5'
  crypton-pem: '>=0.2.4 && <0.4'
  crypton-x509: '>=1.8.0'
  crypton-x509-store: '>=1.8.0'
  crypton-x509-system: '>=1.8.0'
  crypton-x509-validation: '>=1.8.0'
  directory: '>=0'
  memory: '>=0'
  time-hourglass: '>=0'
changelog: |
  # ChangeLog for crypton-x509-util

  ## v1.8.0

  * Depend indirectly on package `time-hourglass`, rather than `hourglass`. Date
    and time-related types and classes are now those from the former package.
    [#18](https://github.com/kazu-yamamoto/crypton-certificate/pull/18)
  * Depend on package `crypton-asn1-types >= 0.4.1` rather than `asn1-types`.
    ASN.1-related types and classes are now those from the former package.
changelog-type: markdown
description: utility to parse, show, validate, sign and produce X509 certificates
  and chain.
description-type: haddock
hash: ef4aabdfa0bbfc22c0ba8ac7714b8fb7e7ff7570fc6355f6a4949a40d45adb6a
homepage: https://github.com/kazu-yamamoto/crypton-certificate
latest: 1.8.0
license-name: BSD-3-Clause
maintainer: Kazu Yamamoto <kazu@iij.ad.jp>
synopsis: Utility for X509 certificate and chain
test-bench-deps: {}
