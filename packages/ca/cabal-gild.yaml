all-versions:
- 0.2.0.1
- 0.2.1.0
- 0.2.1.1
author: Oleg Grenrus <oleg.grenrus@iki.fi>
basic-deps:
  Cabal-syntax: ^>=3.8.1.0 || ^>=3.10.1.0
  base: '>=4.17.2.1 && <4.18 || >=4.18.2.0 && <4.19 || >=4.19.0.0 && <4.20'
  bytestring: ^>=0.11.5.3 || ^>=0.12.0.2
  cabal-gild: '>=0'
  containers: ^>=0.6.7
  directory: ^>=1.3.7.1
  exceptions: ^>=0.10.5
  filepath: ^>=1.4.2.2
  mtl: ^>=2.2.2 || ^>=2.3.1
  parsec: ^>=3.1.16.1
  pretty: ^>=1.1.3.6
changelog: |
  # Change log

  cabal-gild follows the [Package Versioning Policy](https://pvp.haskell.org).
  You can find release notes [on GitHub](https://github.com/tfausak/cabal-gild/releases).
changelog-type: markdown
description: |
  # cabal-gild

  ## Synopsis

  ```sh
  $ cabal install cabal-gild
  $ ...
  $ cabal-gild --inplace example.cabal
  ```

  ## Output

  Turns this...

  ```cabal
  cabal-version: 2.4
  name: cabal-gild
  version: 0

  -- An example formatter
  executable cabal-gild
      default-language: Haskell2010
      hs-source-dirs: src
      main-is: CabalGild.hs
      -- build depends will be in
      -- a nice tabular format
      build-depends: base >=4.11 && <4.13, pretty >=1.1.3.6 && <1.2, bytestring, Cabal ^>=2.5, containers ^>=0.5.11.0 || ^>=0.6.0.1
      -- extensions will be sorted
      other-extensions:
        DeriveFunctor FlexibleContexts ExistentialQuantification OverloadedStrings
        RankNTypes
  ```

  ...into this:

  ```cabal
  cabal-version: 2.4
  name:          cabal-gild
  version:       0

  -- An example formatter
  executable cabal-gild
    default-language: Haskell2010
    hs-source-dirs:   src
    main-is:          CabalGild.hs

    -- build depends will be in
    -- a nice tabular format
    build-depends:
      , base        >=4.11      && <4.13
      , bytestring
      , Cabal       ^>=2.5
      , containers  ^>=0.5.11.0 || ^>=0.6.0.1
      , pretty      >=1.1.3.6   && <1.2

    -- extensions will be sorted
    other-extensions:
      DeriveFunctor
      ExistentialQuantification
      FlexibleContexts
      OverloadedStrings
      RankNTypes
  ```
description-type: markdown
hash: de43f957b10d4781ad074ce8c7ac4b9c54bb74b021b45ff8e57041bd5564fc96
homepage: ''
latest: 0.2.1.1
license-name: GPL-3.0-or-later AND BSD-3-Clause
maintainer: Taylor Fausak
synopsis: Format .cabal files
test-bench-deps:
  Cabal-syntax: '>=0'
  QuickCheck: ^>=2.14.3
  base: ^>=4.17.2.1 || ^>=4.18.2.0 || ^>=4.19.0.0
  bytestring: '>=0'
  cabal-gild: '>=0'
  containers: '>=0'
  filepath: '>=0'
  tasty: ^>=1.5
  tasty-hunit: ^>=0.10.1
  tasty-quickcheck: ^>=0.10.3
