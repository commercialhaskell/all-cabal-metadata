homepage: https://github.com/hvr/cabal-plan
changelog-type: markdown
hash: 7222149c2dad6acde5f31a1d8a1a0abff02d1ced04c4ff2b1a80e37b648dcc90
test-bench-deps: {}
maintainer: hvr@gnu.org
synopsis: Library and utiltity for processing cabal's plan.json file
changelog: ! "# Revision history for `cabal-plan`\n\n## 0.2.0.0\n\n* Add an optional
  `--builddir` argument to all commands and to `findAndDecodePlanJson` function\n*
  Add experimental support for underlining\n* Reimplement CLI with `optparse-applicative`\n*
  Add new sub-command `list-bins` and change semantics of existing `list-bin` sub-cmd\n\n##
  0.1.1.0\n\n* Add `cabal-plan fingerprint` command for printing\n  sha256 sums of
  source tarballs\n\n## 0.1.0.0\n\n* First version. Released on an unsuspecting world.\n"
basic-deps:
  bytestring: ! '>=0.10.0 && <0.11'
  base: ! '>=4.6 && <4.11'
  text: ! '>=1.2.2 && <1.3'
  filepath: ^>=1.3.0 || ^>=1.4.0
  containers: ! '>=0.5.0 && <0.6'
  ansi-terminal: ^>=0.6.2
  mtl: ^>=2.2.1
  base16-bytestring: ^>=0.1.1
  cabal-plan: -any
  optparse-applicative: ^>=0.13.0 || ^>=0.14.0
  aeson: ^>=1.2.0
  directory: ^>=1.2.0 || ^>=1.3.0
all-versions:
- '0.1.0.0'
- '0.1.1.0'
- '0.2.0.0'
author: Herbert Valerio Riedel
latest: '0.2.0.0'
description-type: haddock
description: ! 'This package provides a library for decoding @plan.json@ files as

  well as the simple tool @cabal-plan@ for extracting and pretty printing

  the information contained in the @plan.json@ file.


  @plan.json@ files are generated by [cabal](https://hackage.haskell.org/package/cabal-install)''s
  [nix-style local builds](http://cabal.readthedocs.io/en/latest/nix-local-build.html)
  and contain detailed information about the build/install plan computed by the cabal
  solver.'
license-name: GPL-3
