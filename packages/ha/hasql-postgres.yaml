homepage: https://github.com/nikita-volkov/hasql-postgres
changelog-type: markdown
hash: 9d1dd48a3fb03969faa7ad808fa4634a55541919b74a4c28919aade80be10faf
test-bench-deps:
  criterion-plus: ==0.1.*
  hasql: ==0.7.*
  either: -any
  bytestring: ! '>=0.10 && <0.11'
  base-prelude: ! '>=0.1.3 && <0.2'
  base: -any
  time: ! '>=1.4 && <1.6'
  HDBC: ==2.4.*
  hspec: ==2.1.*
  text: ! '>=1 && <1.3'
  list-t: <0.5
  monad-control: ! '>=0.3 && <1.1'
  filepath: ! '>=1.3 && <1.5'
  doctest: ==0.9.*
  HDBC-postgresql: ==2.3.*
  hasql-postgres: -any
  postgresql-binary: -any
  quickcheck-instances: ==0.3.*
  mtl-prelude: <3
  old-locale: ==1.0.*
  postgresql-simple: ==0.4.*
  hashable: ! '>=1.2 && <1.3'
  deepseq: ==1.*
  scientific: ! '>=0.2 && <0.4'
  QuickCheck: ==2.7.*
  hasql-backend: -any
  aeson: -any
  vector: -any
  directory: ==1.2.*
maintainer: Nikita Volkov <nikita.y.volkov@mail.ru>
synopsis: A "PostgreSQL" backend for the "hasql" library
changelog: ! '# 0.10.3.1

  * "attoparsec"-0.13 support


  # 0.10.3

  * GHC 7.10 support


  # 0.10.2

  * JSON support


  # 0.10.1

  * Update the "backend" dependency


  # 0.10.0 - Major overhaul

  * Transactions now are only retried in case of the "serialization_failure" (40001)
  error


  # 0.9.1

  * The Unknown type got implemented


  # 0.9.0

  * The unit result handler no longer fails on statements, which do produce results

  * The dependency on "list-t" got relaxed

  * The dependency on "either" got restricted

'
basic-deps:
  free: ! '>=4.6 && <5'
  either: ! '>=4.3 && <5'
  bytestring: ! '>=0.10 && <0.11'
  base-prelude: ! '>=0.1.13 && <0.2'
  time: ! '>=1.4 && <1.6'
  postgresql-libpq: ==0.9.*
  text: ! '>=1 && <1.3'
  uuid: ==1.3.*
  list-t: <0.5
  loch-th: ==0.2.*
  postgresql-binary: ==0.5.*
  hashtables: ! '>=1.1 && <1.3'
  mmorph: ==1.0.*
  hashable: ! '>=1.2 && <1.3'
  placeholders: ==0.1.*
  attoparsec: ! '>=0.10 && <0.14'
  transformers: ! '>=0.3 && <0.5'
  scientific: ! '>=0.2 && <0.4'
  hasql-backend: ==0.4.*
  aeson: ! '>=0.7 && <0.11'
  template-haskell: ==2.*
  vector: ! '>=0.10 && <0.12'
all-versions:
- 0.1.0
- 0.1.1
- 0.1.2
- 0.1.3
- 0.2.0
- 0.3.0
- 0.3.1
- 0.3.2
- 0.3.3
- 0.4.0
- 0.5.0
- 0.5.1
- 0.6.0
- 0.6.1
- 0.7.0
- 0.7.1
- 0.7.2
- 0.7.3
- 0.8.0
- 0.9.0
- 0.9.1
- 0.10.0
- 0.10.1
- 0.10.2
- 0.10.3
- 0.10.3.1
- 0.10.3.2
- 0.10.3.3
- 0.10.4.0
- 0.10.5
- 0.10.6
author: Nikita Volkov <nikita.y.volkov@mail.ru>
latest: 0.10.6
description-type: haddock
description: ! 'This library provides a \"PostgreSQL\" driver for

  <http://hackage.haskell.org/package/hasql the "hasql" library>.


  It supports all Postgres versions starting from 8.3

  and is tested against 8.3, 9.3 and 9.4

  with the @integer_datetimes@ setting off and on.


  According to the included benchmarks,

  it performs up to 2x faster than \"postgresql-simple\" and

  up to 7x faster than \"HDBC\".

  You can read up

  <http://nikita-volkov.github.io/hasql-benchmarks/ a post with analysis of those
  benchmarks>.'
license-name: MIT
