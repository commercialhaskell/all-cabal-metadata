all-versions:
- 0.1.0.0
- 0.1.0.1
- 0.2.0.0
- 0.2.0.1
- 0.3.0.0
- 0.3.0.1
- 0.3.0.2
author: Tim Baumann
basic-deps:
  aeson: '>=0.11 && <1.6'
  base: '>=4.6 && <5'
  containers: '>=0.5 && <0.7'
  data-default: '>=0.4 && <0.8'
  diagrams-lib: '>=1.3 && <1.5'
  grid: '>=7.7.1 && <7.9'
changelog: |
  # Change Log

  ## 0.3.0.2 (2021-05-09)

  - Relax upper bounds: Allow `aeson-1.5.*`

  ## 0.3.0.1 (2019-01-03)

  - Relax upper bounds: Allow `containers-0.6.*`, `aeson-1.4.*`

  ## 0.3.0.0 (2017-04-03)

  - The pieces on the board are now identifiable by a number from 1 to 15 (for each team); added `Piece` datatype
  - Rename `teamRating` to `rateTeam` in `Game.Halma.AI.Base`
  - Introduce new type `Move` representing moves on the board
  - changed call signature of `movePiece`
  - nicer default colours
  - removed `size :: GridSize` type index from `HalmaGrid` and `HalmaBoard`

  ## 0.2.0.1 (2015-07-22)

  - Relax upper bounds: Allow `mvc-1.1.*`

  ## 0.2.0.0

  - Halma AI added
  - More rule options
  - Upgraded to `diagrams-1.3`

  ## 0.1.0.1

  - Cabal file fix

  ## 0.1.0.0

  - Initial release
changelog-type: markdown
description: Rules and `diagrams`-based renderer for the board game Halma on a hexagonal
  grid.
description-type: haddock
hash: f19afd7d706a6a9cfc6b64ccccb70ae6d5b0b0f93112de66f6a6630a4894bdc6
homepage: https://github.com/timjb/halma
latest: 0.3.0.2
license-name: MIT
maintainer: tim@timbaumann.info
synopsis: Library implementing Halma rules
test-bench-deps:
  HUnit: '>=0'
  QuickCheck: '>=0'
  base: '>=0'
  containers: '>=0'
  grid: '>=0'
  halma: '>=0'
  test-framework: '>=0.6 && <0.9'
  test-framework-hunit: '>=0.3.0 && <0.4'
  test-framework-quickcheck2: '>=0.3.0 && <0.4'
