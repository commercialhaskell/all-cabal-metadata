homepage: http://github.com/achudnov/haxy
changelog-type: text
hash: 583199a724d204d453a0cdbff61d446076443ea9c9c3ce89a5c62b7ba563bd49
test-bench-deps: {}
maintainer: Andrey Chudnov <oss@chudnov.com>
synopsis: A simple HTTP proxy server library
changelog: ! '=1.0.1=

  * Fixed a bug where request is bypassed. Thanks Scott Peter Johnson!


  =1.0=

  * Changed the settings to use the Logger from http-server.

  * The default logger is now http-server''s default logger.

  * Added extensive event logging.


  =0.9.1=

  * Packaging fixes


  =0.9=

  * Initial release.

'
basic-deps:
  bytestring: ! '>=0.9 && <0.11'
  hostname: ! '>=1.0 && <2'
  base: ! '>=4.3.1.0 && <5'
  data-default-class: ! '>=0.0.1 && <0.1'
  http-server: ! '>=1.0.6 && <1.1'
  url: ! '>=2.1.2 && <2.2'
  mtl: ==2.*
  HTTP: ! '>=4000.2.0 && <5000'
all-versions:
- 1.0.1
author: Andrey Chudnov
latest: 1.0.1
description-type: haddock
description: A library for writing HTTP proxy servers with the focus on simplicity,
  flexibility and modularity. Allows arbitrary transformations on requests and responses
  and custom caching methods. It's up to the user to make sure that the message transformations
  are consistent with the HTTP specification. Note that the package versions follow
  Semantic Versioning model (semver.org).
license-name: BSD-3-Clause
