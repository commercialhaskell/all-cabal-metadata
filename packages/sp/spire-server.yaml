all-versions:
- 0.1.0.0
author: Josh Burgess
basic-deps:
  async: '>=2.2 && <2.3'
  attoparsec: '>=0.14 && <0.15'
  base: '>=4.20 && <5'
  bytestring: '>=0.11 && <0.13'
  case-insensitive: '>=1.2 && <1.3'
  data-default-class: '>=0.1 && <0.2'
  http-core: '>=0.1 && <0.2'
  http-semantics: '>=0.2 && <0.3'
  http-types: '>=0.12 && <0.13'
  http2: '>=5.3 && <5.5'
  network: '>=3.1 && <3.3'
  network-run: '>=0.4 && <0.5'
  spire: '>=0.1 && <0.2'
  text: '>=2.0 && <2.2'
  time-manager: '>=0.1 && <0.2'
  tls: '>=2.0 && <2.2'
changelog: |
  # Revision history for spire-server

  ## 0.1.0.0 -- 2026-04-27

  * Initial release. Hand-rolled HTTP server backend for spire: parses
    requests, dispatches to a Service, and renders responses without
    pulling in warp or wai.
changelog-type: markdown
description: |-
  A lightweight HTTP server that speaks spire Service directly.
  No WAI, no warp: just network sockets, HTTP parsing, and dispatch
  to @Service IO (Request Body) (Response Body)@.
  .
  Supports HTTP/1.1 (custom parser), HTTP/2 (via the @http2@ package),
  TLS with ALPN protocol negotiation, and automatic HTTP/1.1 vs HTTP/2
  detection.
  .
  Proves the zero-WAI path: the entire acolyte stack
  (including gRPC) can run without any WAI dependency.
description-type: haddock
hash: a1605cae9f821ae8edc501ec36d34714b2b81642aca8796125b2c36f2e3fdeef
homepage: https://github.com/joshburgess/acolyte
latest: 0.1.0.0
license-name: BSD-3-Clause
maintainer: Josh Burgess <joshburgess.webdev@gmail.com>
synopsis: Spire-native HTTP/1.1 + HTTP/2 server, no WAI dependency
test-bench-deps:
  base: '>=4.20 && <5'
  bytestring: '>=0.11 && <0.13'
  case-insensitive: '>=1.2 && <1.3'
  hedgehog: '>=1.4 && <1.8'
  http-client: '>=0.7 && <0.8'
  http-core: '>=0.1 && <0.2'
  http-types: '>=0.12 && <0.13'
  network: '>=3.1 && <3.3'
  spire: '>=0.1 && <0.2'
  spire-server: '>=0.1 && <0.2'
  text: '>=2.0 && <2.2'
