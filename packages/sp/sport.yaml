all-versions:
- 0.1.0.0
- 0.2.0.0
- 1.0.0.0
- 2.0.0.0
- 3.0.0.0
- 3.1.0.0
author: Standard Semiconductor
basic-deps:
  async: '>=0'
  base: <5
  bytestring: '>=0'
  sport: '>=0'
  stm: '>=0'
  unix: '>=0'
changelog: |
  # Revision history for sport

  ## 3.1.0.0 -- 7-25-2025

  * Fix exclusive flag, was not being set

  ## 3.0.0.0 -- 6-7-2026

  * Update `withSport`, add `withNewSport`
  * Remove `runSport` daemon

  ## 2.0.0.0 -- 6-6-2026

  * Add `flushSport`, `withOpenSport`
  * Add `displaySportException`, `SportException` instances
  * Improve concurrency and resource handling
  * Add `getOpenSportCfg`

  ## 1.0.0.0 -- 4-30-2026

  * Fix `SportAlreadyOpen` bug
  * Update exception types
  * Add `isOpenSport` to STM API

  ## 0.2.0.0 -- 4-21-2026

  * Expose current configuration in STM
  * Re-export baud rate from unix package

  ## 0.1.0.0 -- 4-20-2026

  * First version. Released on an unsuspecting world.
changelog-type: markdown
description: |
  # Sport

  UNIX Serial Port

  ```hs
  import Sport

  main = do
    s <- newSportIO
    openSport s defSportCfg{path="/dev/ttyUSB1"}
    bs <- readSport s 64
    writeSport s bs
  ```

  Development

  ```
  cabal build
  cabal haddock
  ```
description-type: markdown
hash: d6a5e29b74cf472cb85d22b72ba87b501da4617db59983045e140511ca44d2ec
homepage: ''
latest: 3.1.0.0
license-name: MIT
maintainer: standardsemiconductor@gmail.com
synopsis: UNIX serial port
test-bench-deps:
  async: '>=0'
  base: '>=0'
  bytestring: '>=0'
  sport: '>=0'
