all-versions:
- 1.0.0.0
author: Pete Ryland, based on regex-pcre by Christopher Kuklewicz
basic-deps:
  array: '>=0.3 && <0.6'
  base: '>=4.3 && <5'
  bytestring: '>=0.9 && <0.13'
  containers: '>=0.4 && <0.8'
  regex-base: '>=0.94 && <0.95'
changelog: |
  See also http://pvp.haskell.org/faq

  ## 1.0.0.0

  - Migrate from the obsolete pcre3 library to the new (confusingly-named) pcre2 one

  ## 0.95.0.0 revision 6

  - Allow `containers-0.7`

  ## 0.95.0.0 revision 5

  - Allow `bytestring-0.12`

  ## 0.95.0.0 revision 4

  - Compatibility with `base >= 4.17` (GHC 9.4)

  ## 0.95.0.0 revision 3

  - Compatibility with `base-4.16` (GHC 9.2)

  ## 0.95.0.0 revision 2

  - Compatibility with `base-4.15` (GHC 9.0) and `bytestring-0.11`

  ## 0.95.0.0

  - Update to `regex-0.94.0.0` API
  - Compatibility with `base-4.13.0`
  - Use `pkg-config` for locating `pcre`

  ----
changelog-type: markdown
description: This package provides a <http://pcre.org/ PCRE2> backend for the <//hackage.haskell.org/package/regex-base
  regex-base> API.
description-type: haddock
hash: 241258b9c532aa0d800e7442b52e473520eeae7047e8504387aeb71efd1c288d
homepage: https://dev.piglet.ch/pdr/regex-pcre2
latest: 1.0.0.0
license-name: BSD-3-Clause
maintainer: Pete Ryland <pdr@piglet.ch>
synopsis: PCRE2 Backend for "Text.Regex" (regex-base)
test-bench-deps:
  HUnit: '>=1.6 && <1.7'
  base: '>=4.3 && <5'
  bytestring: '>=0.9 && <0.13'
  regex-pcre2: '>=0'
  utf8-string: '>=1.0 && <1.1'
