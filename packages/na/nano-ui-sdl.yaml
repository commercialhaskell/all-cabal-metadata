all-versions:
- 0.1.0.0
author: goolord
basic-deps:
  base: '>=4.20 && <4.23'
  bytestring: '>=0.11 && <0.13'
  containers: '>=0.6.7 && <0.9'
  dir-traverse: '>=0.2.3 && <0.3'
  directory: '>=1.3.7 && <1.4'
  effectful-core: '>=2.5 && <2.8'
  file-embed: '>=0.0.16 && <0.1'
  filepath: '>=1.4.100 && <1.6'
  hashable: '>=1.4 && <1.6'
  nano-ui: '>=0.1 && <0.2'
  nano-ui-sdl: '>=0'
  primitive: '>=0.8 && <0.10'
  record-hasfield: '>=1.0 && <1.1'
  sdl3-bindgen-sys: '>=0.0.0.3 && <0.0.1'
  text: '>=2.1.2 && <2.2'
  unordered-containers: '>=0.2.19 && <0.3'
changelog: |
  # Changelog

  ## 0.1.0.0

  First release.

  - `runSdlApp` and `runSdlAppReduce` run nano-ui views in an SDL3 window.
  - Text shaped with SDL_ttf and HarfBuzz, drawn from the bundled Inter font
    or installed fonts looked up by family name, with fallback fonts for
    scripts the UI font lacks.
  - Clipboard, cursors, display scaling, images, and native file dialogs.
  - The `simd` flag compiles the draw-batch culler with AVX2 on x86-64.
changelog-type: markdown
description: |
  # nano-ui-sdl

  SDL3 window backend for [nano-ui](https://github.com/goolord/nano-ui). Text is
  shaped with SDL_ttf and HarfBuzz, drawn from the bundled Inter font or installed
  fonts looked up by family name, with fallback fonts for other scripts. The
  backend also opens native file dialogs.

  ```haskell
  {-# LANGUAGE OverloadedStrings #-}

  import NanoUI
  import NanoUI.Backend.Sdl (defaultSdlOptions, runSdlApp)

  main :: IO ()
  main = runSdlApp defaultSdlOptions (label "Hello")
  ```

  `runSdlAppReduce` runs a view against a model and an update function, for use
  with `NanoUI.Emit`. `SdlOptions` sets the window, fonts, font size, theme, and
  vsync.

  ## Running

  ```sh
  cabal run nano-ui-sdl-anim     # tween and spring animations
  cabal bench nano-ui-sdl-bench  # runFrame and SDL drawing timings
  ```

  ## Requirements

  SDL3 and SDL3_ttf 3.2 or later, and pkg-config. The backend is behind the `sdl`
  flag, which is on by default.

  On x86-64, `-f simd` compiles the draw-batch culler with AVX2. A binary built
  that way needs an AVX2 CPU.
description-type: markdown
hash: 01c4e9f5111b81a454b4dc524ab6df88bd9863005631351244d7e16e058ef572
homepage: https://github.com/goolord/nano-ui
latest: 0.1.0.0
license-name: MIT AND OFL-1.1
maintainer: zacharyachurchill@gmail.com
synopsis: SDL3 window backend for nano-ui
test-bench-deps:
  base: '>=4.20 && <4.23'
  containers: '>=0.6.7 && <0.9'
  dir-traverse: '>=0.2.3 && <0.3'
  directory: '>=1.3.7 && <1.4'
  filepath: '>=1.4.100 && <1.6'
  nano-ui: '>=0'
  nano-ui-sdl: '>=0'
  primitive: '>=0.8 && <0.10'
  tasty-bench: '>=0.3 && <0.6'
  text: '>=2.1.2 && <2.2'
