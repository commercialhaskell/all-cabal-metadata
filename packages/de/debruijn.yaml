all-versions:
- '0.1'
- '0.2'
- '0.3'
author: Oleg Grenrus <oleg.grenrus@iki.fi>
basic-deps:
  base: ^>=4.16.3.0 || ^>=4.17.0.0 || ^>=4.18.0.0 || ^>=4.19.0.0 || ^>=4.20.0.0
  deepseq: ^>=1.4.6.1 || ^>=1.5.0.0
  fin: ^>=0.3.1
  skew-list: ^>=0.1
  some: ^>=1.0.6
  transformers: ^>=0.5.6.2 || ^>=0.6.1.0
changelog: |
  # 0.3

  - Remove RenameA machinery
  - Introduce `Contract` for environment contraction (reverse of weakening)
  - `keepSub` only requires `Weaken`, not `Rename`.
changelog-type: markdown
description: |-
  de Bruijn indices and levels for well-scoped terms.

  This is "unsafe" (as it uses 'unsafeCoerce') implementation, but it's fast.
  The API is the same as in @debruin-safe@ package.
description-type: haddock
hash: 1fd6c63d332289bb9090d68106ef92d779cd7c72480c3b16cba2025cc1bc73ef
homepage: ''
latest: '0.3'
license-name: BSD-3-Clause
maintainer: Oleg Grenrus <oleg.grenrus@iki.fi>
synopsis: de Bruijn indices and levels
test-bench-deps: {}
