all-versions:
- '1'
- 1.0.1
- 1.0.2
- '1.1'
- 1.1.1
author: Oleg Grenrus <oleg.grenrus@iki.fi>
basic-deps:
  base: '>=4.12 && <4.21'
changelog: |
  ## 1.1.1

  - Drop support for GHC prior GHC-8.6.5

  ## 1.1

  - Depend on `bifunctor-classes-compat` only.
    Instances for types defined in `bifunctors` package are moved there.
    With this change `assoc` only depends on `base` and `tagged`.
  - Mark modules as explicitly Safe

  ## 1.0.2

  - Add 'Swap' instances for more n-tuples

  ## 1.0.1

  - Add `Assoc Const` and `Tagged` instances
changelog-type: markdown
description: |-
  Provides generalisations of
  @swap :: (a,b) -> (b,a)@ and
  @assoc :: ((a,b),c) -> (a,(b,c))@
  to
  @Bifunctor@s supporting similar operations (e.g. @Either@, @These@).
description-type: haddock
hash: ad458c9e06a23dd275f8dbb02d13e250b8e40ac4858bc67e7992628563d14f5d
homepage: ''
latest: 1.1.1
license-name: BSD-3-Clause
maintainer: Oleg Grenrus <oleg.grenrus@iki.fi>
synopsis: 'swap and assoc: Symmetric and Semigroupy Bifunctors'
test-bench-deps: {}
