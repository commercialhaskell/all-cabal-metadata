all-versions:
- 0.1.0.0
- 0.1.0.1
author: Chris Martin
basic-deps:
  base: ^>=4.9 || ^>=4.10 || ^>=4.11 || ^>=4.12 || ^>=4.13 || ^>=4.14 || ^>=4.15 ||
    ^>=4.16
  contravariant: ^>=1.5
changelog: |
  # Changelog

  ## v1.0.0.0

  Initial version

  ## v1.0.0.1 - August 9, 2021

  Rename the hedgehog test executable to support
  case-insensitive file systems

  Remove the doctest test suite

  Tighten previously-vague dependency version bounds

  Cabal-version raised to 3.0
changelog-type: markdown
description: |
  # assoc-list

  An association list conceptually signifies a mapping, but is represented as a list (of key-value pairs).

  This package defines an association list as a type alias for a list of tuples.

  ```haskell
  type AssocList a b = [(a, b)]
  ```
description-type: markdown
hash: 9300f7fcff2b535958a0458fe5482ceaefc896d09e0c3332f9ec700d28b497ba
homepage: https://github.com/typeclasses/assoc-list
latest: 0.1.0.1
license-name: MIT
maintainer: Chris Martin, Julie Moronuki
synopsis: Association lists (lists of tuples)
test-bench-deps:
  assoc-list: '>=0'
  base: ^>=4.9 || ^>=4.10 || ^>=4.11 || ^>=4.12 || ^>=4.13 || ^>=4.14 || ^>=4.15 ||
    ^>=4.16
  contravariant: ^>=1.5
  hedgehog: ^>=1.0 || ^>=1.1
