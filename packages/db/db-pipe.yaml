all-versions:
- 0.1.0.1
author: Dmitry Zuykov
basic-deps:
  base: '>=4.17 && <5'
  clock: '>=0.8 && <0.9'
  interpolatedstring-perl6: '>=1.0 && <1.1'
  mtl: '>=2.3 && <2.4'
  sqlite-simple: '>=0.4.18 && <0.5'
  stm: '>=2.5 && <2.6'
  text: '>=2.0 && <2.2'
  unliftio: '>=0.2.24 && <0.3'
changelog: |
  # Revision history for db-pipe

  ## 0.1.0.1  2026-06-08

  First Hackage release. Previously this package only lived as a
  vendored subtree inside the `hbs2` repository; the source moves to
  `github.com/NCrashed/db-pipe` for independent maintenance.

    - No source changes from the vendored 0.1.0.1.
    - Cabal metadata filled in: synopsis, description, homepage,
      bug-reports, source-repository, `tested-with`, explicit version
      bounds on every dependency.
    - Maintainer is now `Anton Gushcha <ncrashed@gmail.com>`;
      original authorship by Dmitry Zuykov is preserved.
changelog-type: markdown
description: |
  # db-pipe

  A pipe-like wrapper around [`sqlite-simple`](https://hackage.haskell.org/package/sqlite-simple).

  ## What it does

  `db-pipe` wraps `sqlite-simple` in a small monadic pipe abstraction.
  A long-lived `DBPipeEnv` owns a connection, a transaction handle,
  and a couple of STM channels; individual queries run inside a
  `DBPipeM` monad that batches `select`, `update`, `insert`, and
  `ddl` statements into explicit transactional blocks
  (`transactional`, `transactional_`, `commitAll`).

  ## Where it came from

  Originally written by Dmitry Zuykov (voidlizard) inside the
  [`hbs2`](https://github.com/NCrashed/hbs2) project. This Hackage
  release is published from `github.com/NCrashed/db-pipe` so
  downstream projects can depend on it without vendoring.

  ## License

  BSD-3-Clause; see `LICENSE`. Copyright holders are listed in the
  cabal file.
description-type: markdown
hash: 21dbb73144ea1f829be48a963b974044fedc3ef4a5c8f0462aca85b6dec5a056
homepage: https://github.com/NCrashed/db-pipe
latest: 0.1.0.1
license-name: BSD-3-Clause
maintainer: Anton Gushcha <ncrashed@gmail.com>
synopsis: A pipe-like wrapper around sqlite-simple
test-bench-deps: {}
