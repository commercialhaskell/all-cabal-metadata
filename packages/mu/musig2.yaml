all-versions:
- 0.1.0
- 0.1.1
- 0.1.2
- 0.1.3
- 0.1.4
author: Jose Storopoli
basic-deps:
  base: '>=4.19 && <5'
  base16-bytestring: '>=1.0 && <1.1.0'
  binary: '>=0.8 && <0.9'
  bytestring: '>=0.9 && <0.13'
  containers: '>=0.6 && <0.9'
  entropy: '>=0.4 && <0.5'
  ppad-hmac-drbg: '>=0.2 && <0.3'
  ppad-secp256k1: '>=0.5 && <0.6'
  ppad-sha256: '>=0.3 && <0.4'
changelog: |
  # Changelog

  ## 0.1.3 (2026-01-04)

  * Performance improvements:
    - Cache `KeyAggContext` in `SessionContext` to avoid redundant EC operations
    - Replace lazy `foldl` with strict `foldl'` to prevent space leaks
    - Add strictness annotations to `SessionContext` fields
    - Replace O(n) list indexing with O(log n) `Seq.lookup` in `partialSigVerify`
    - Optimize `integerToBytes32` to eliminate intermediate list allocation
    - Fix `NFData` instances in benchmarks for accurate measurements

  ## 0.1.2 (2025-12-30)

  * Fix Hackage build failure by implementing `modQ` locally to support `ppad-secp256k1` 0.5.0+.

  ## 0.1.1 (2025-12-22)

  * Bump `ppad-secp256k1` to 0.5.0. More efficient and faster EC operations.

  ## 0.1.0 (2025-09-20)

  * Initial release, full BIP-0327 support except [Deterministic and Stateless Signing for a Single Signer](https://github.com/bitcoin/bips/blob/master/bip-0327.mediawiki#user-content-Deterministic_and_Stateless_Signing_for_a_Single_Signer)
changelog-type: text
description: Pure BIP0327 MuSig2 (partial)signatures with tweak support on the elliptic
  curve secp256k1.
description-type: haddock
hash: d814d448009ec93d4ed794971bd00d748e056a0e9d4cf92e9c7afd8b25599d08
homepage: https://github.com/storopoli/musig2
latest: 0.1.4
license-name: MIT
maintainer: jose@storopoli.com
synopsis: MuSig2 library
test-bench-deps:
  base: '>=4.19 && <5'
  base16-bytestring: '>=0'
  binary: '>=0'
  bytestring: '>=0'
  containers: '>=0'
  criterion: '>=0'
  deepseq: '>=0'
  entropy: '>=0'
  musig2: '>=0'
  ppad-secp256k1: '>=0'
  ppad-sha256: '>=0'
  tasty: '>=0'
  tasty-hunit: '>=0'
  tasty-quickcheck: '>=0'
