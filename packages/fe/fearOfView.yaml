all-versions:
- 0.1.0.0
- 0.1.0.1
author: mbays
basic-deps:
  MonadRandom: '>=0.6 && <0.7'
  astar: '>=0.3 && <0.4'
  base: '>=4.3 && <5'
  bytestring: '>=0.10 && <0.13'
  containers: '>=0.4 && <0.9'
  directory: '>=1.2.3.0 && <1.4'
  filelock: '>=0.1 && <0.2'
  filepath: '>=1.0 && <2.1'
  hashable: '>=1.4 && <1.6'
  hscurses: '>=1.4 && <1.6'
  mtl: '>=2.2 && <2.4'
  random: '>=1.3 && <1.4'
  safe: '>=0.3.18 && <0.4'
  safe-exceptions: '>=0.1 && <0.2'
  serialise: '>=0.2 && <0.3'
  splitmix: '>=0.1 && <0.2'
  unordered-containers: '>=0.2 && <0.3'
changelog: |
  # Revision history for fearOfView

  ## 0.1.0.0 -- 2025-08-10

  * First release.
changelog-type: markdown
description: |
  # Fear of View
  A terminal broughlike game about manipulating vision.

  Released under the AGPL, version 3 or later. See LICENSE.

  ![Screenshot](fov-shot.png "Screenshot")

  ## Building
  Dependencies:
  * GHC and cabal: package `cabal-install` on Debian, `dev-haskell/cabal` on Gentoo, etc.
  * ncurses

  Run with `cabal run`.
  Install with `cabal install`.
description-type: markdown
hash: 1d6180a6746a94afd03bea00e5657ee310e2d7eada0f7c2f65b2c077575fe2f3
homepage: https://mbays.sdf.org/fov/
latest: 0.1.0.1
license-name: AGPL-3.0-or-later
maintainer: mbays@sdf.org
synopsis: A terminal broughlike game about manipulating vision
test-bench-deps: {}
