homepage: https://gitlab.com/k0001/xmlbf
changelog-type: markdown
hash: c0a7e38f611e4090e05debe22fee75e68aa97d59178b3098de3ca7751bf66709
test-bench-deps:
  bytestring: -any
  base: -any
  unordered-containers: -any
  text: -any
  quickcheck-instances: -any
  xmlbf: -any
  tasty-quickcheck: -any
  tasty-hunit: -any
  transformers: -any
  tasty: -any
  QuickCheck: -any
maintainer: ren§ren*zone
synopsis: XML back and forth! Parser, renderer, ToXml, FromXml, fixpoints.
changelog: ! "# 0.4.1\n\n* Generalized type of `pRead`.\n\n\n# 0.4\n\n* BREAKING CHANGE.
  `pElement` now skips leading whitespace before an element.\n\n* Fixed nested element
  parsing (#6)\n\n\n# 0.3\n\n* BREAKING CHANGE. Renamed `df` and `dfM` to `dfpos`
  and `dfposM` respectively.\n\n* Added `dfpre` and `dfpreM`.\n\n* Improved `Show`
  instance for `Node`.\n\n* Added `element'`.\n\n\n# 0.2\n\n* `Text` constructor hidden
  in favor of a `text` function plus a `Text` pattern\n  synonym, just like with `element`
  and `Element`.\n\n* Documentation: Render `Element` pattern synonym.\n\n\n# 0.1\n\n*
  First version.\n"
basic-deps:
  bytestring: -any
  base: <5
  unordered-containers: -any
  text: -any
  containers: -any
  transformers: -any
all-versions:
- '0.4'
- '0.4.1'
author: Renzo Carbonara
latest: '0.4.1'
description-type: markdown
description: ! '# xmlbf


  _XML back and forth!_


  `xmlbf` provides `FromXML` and `ToXML` instances intended to be used as the

  familiar `FromJSON` and `ToJSON` from the `aeson` package.


  Build by running `nix-build` in the top-level directory. Alternatively, running

  `nix-shell` in the top-level directory puts you in a shell where all of the

  dependencies of this project are available. There you can use `cabal` and/or

  `./Setup.hs` as usual.

'
license-name: Apache-2.0
