homepage: http://bitbucket.org/wuzzeb/couchdb-enumerator
changelog-type: ''
hash: 00eb32c1e6ca997c893ac1f92381c893bed644cdbd82c00c652c1bbaa1f2f72f
test-bench-deps:
  test-framework-hunit: -any
  bytestring: ! '>=0.9 && <0.10'
  test-framework: ! '>=0.4.1'
  couchdb-enumerator: -any
  base: ! '>=4 && <5'
  unordered-containers: ! '>=0.1 && <0.3'
  text: ! '>=0.11 && <0.12'
  monad-control: ! '>=0.3 && <0.4'
  test-framework-quickcheck2: -any
  lifted-base: ! '>=0.1 && <0.2'
  HUnit: ! '>=1.2 && <2'
  attoparsec-enumerator: ! '>=0.2 && <0.4'
  http-enumerator: ! '>=0.7 && <0.8'
  enumerator: ! '>=0.4 && <0.5'
  utf8-string: ! '>=0.3 && <0.4'
  attoparsec: ! '>=0.8 && <0.11'
  transformers: ! '>=0.2 && <0.3'
  QuickCheck: ! '>=2.4'
  http-types: ! '>=0.6 && <0.7'
  aeson: ! '>=0.5 && <0.7'
  vector: -any
maintainer: John Lenz <lenz@math.uic.edu>
synopsis: Couch DB client library using http-enumerator and aeson
changelog: ''
basic-deps:
  bytestring: ! '>=0.9 && <0.10'
  base: ! '>=4 && <5'
  unordered-containers: ! '>=0.1 && <0.3'
  text: ! '>=0.11 && <0.12'
  monad-control: ! '>=0.3 && <0.4'
  lifted-base: ! '>=0.1 && <0.2'
  attoparsec-enumerator: ! '>=0.2 && <0.4'
  http-enumerator: ! '>=0.7 && <0.8'
  enumerator: ! '>=0.4 && <0.5'
  utf8-string: ! '>=0.3 && <0.4'
  attoparsec: ! '>=0.8 && <0.11'
  transformers: ! '>=0.2 && <0.3'
  http-types: ! '>=0.6 && <0.7'
  aeson: ! '>=0.5 && <0.7'
all-versions:
- 0.2.0
- 0.2.1
- 0.3.0
- 0.3.1
- 0.3.2
- 0.3.3
- 0.3.4
- 0.3.5
- 0.3.6
- 0.3.7
author: John Lenz <lenz@math.uic.edu>
latest: 0.3.7
description-type: haddock
description: ! 'This package is a thin wrapper around http-enumerator to access a
  Couch DB Database,

  using the aeson package to parse and encode JSON data.  http-enumerator, aeson,
  and

  attoparsec fit togther so well that this package is mostly just a direct combination

  of these packages.  The single additional feature in this package is an attoparsec
  parser

  for views, which allows constant memory processing of view returns.

  This package is deprecated by <http://hackage.haskell.org/package/couchdb-conduit>'
license-name: BSD-3-Clause
