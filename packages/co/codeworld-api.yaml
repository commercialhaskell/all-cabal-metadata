homepage: ''
changelog-type: ''
hash: 3549d453f414364b822a8619abf332f685285dffb1bdca7d10da5ecf6a550e4f
test-bench-deps: {}
maintainer: Chris Smith <cdsmith@gmail.com>
synopsis: Graphics library for CodeWorld
changelog: ''
basic-deps:
  cereal-text: ! '>=0.1.0 && <0.2'
  cereal: ! '>=0.5.4 && <0.6'
  base: ! '>=4.9 && <5'
  time: ! '>=1.6.0 && <1.9'
  text: ! '>=1.2.2 && <1.3'
  random-shuffle: ! '>=0.0.4 && <0.1'
  containers: ! '>=0.5.7 && <0.6'
  blank-canvas: ! '>=0.6 && <0.7'
  mtl: ! '>=2.2.1 && <2.3'
  hashable: ! '>=1.2.4 && <1.3'
  random: ! '>=1.1 && <1.2'
all-versions:
- '0.1.0.1'
- '0.2.0.0'
- '0.2.1.0'
- '0.2.2.0'
- '0.2.2.1'
- '0.2.3'
author: The CodeWorld Authors
latest: '0.2.3'
description-type: haddock
description: ! 'This module provides the drawing code for CodeWorld.  It is heavily
  inspired by

  Gloss, but modified for consistency and pedagogical reasons.


  It comes with two backends. When compiled with GHCJS, it uses the JavaScript FFI
  to

  run on <http://code.world/>.  When compiled with GHC, it uses the blank-canvas

  package to provide a webpage consisting of just a panel locally. This way, the same

  program that runs on the CodeWorld server can also be run locally.'
license-name: Apache
