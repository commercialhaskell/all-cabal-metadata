all-versions:
- 0.1.0.0
- 0.1.1.0
author: Tom Sydney Kerckhove
basic-deps:
  QuickCheck: '>=0'
  aeson: '>=0'
  autodocodec: '>=0'
  base: '>=4.7 && <5'
  bytestring: '>=0'
  containers: '>=0'
  fast-myers-diff: '>=0.0.1'
  genvalidity: '>=0'
  genvalidity-containers: '>=0'
  genvalidity-path: '>=0'
  genvalidity-text: '>=0'
  path: '>=0'
  path-io: '>=0'
  safe-coloured-text: '>=0'
  text: '>=0'
  unordered-containers: '>=0'
  vector: '>=0'
changelog: |
  # Changelog

  ## [0.1.1.0] - 2026-07-29

  ### Fixed

  * `writeManifestTxtFile` now writes UTF-8.  It wrote through a binary handle
    with `hPutStr`, which truncates every `Char` to its low byte, so any
    non-ASCII character in a rendered source line came out as a different
    character (`…` as `&`) or as an invalid UTF-8 byte.  The `.json` manifest
    was unaffected, as is the driver's `report.txt`, which already encoded
    explicitly.

  ## [0.1.0.0] - 2026-07-16

  * First released version.
changelog-type: markdown
description: Runtime support library for sydtest's mutation testing. It provides the
  manifest, coverage and identifier types shared between the core sydtest library,
  the mutation plugin, and the mutation driver. See https://github.com/NorfairKing/sydtest#readme
  for more information.
description-type: haddock
hash: 206ff334282334a2a35a6c490fc5c60349927563b5a5fb9f979e8389df571daa
homepage: https://github.com/NorfairKing/sydtest#readme
latest: 0.1.1.0
license-name: LicenseRef-OtherLicense
maintainer: syd@cs-syd.eu
synopsis: Runtime support library for sydtest's mutation testing
test-bench-deps: {}
