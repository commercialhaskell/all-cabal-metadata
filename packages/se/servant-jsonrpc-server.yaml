homepage: https://github.com/bitnomial/servant-jsonrpc
changelog-type: markdown
hash: 410e01962fc30a8d047d146eeeb15476aea0468fea4cd8a95ceeca6be9dd3c62
test-bench-deps: {}
maintainer: Ian Shipman <ics@gambolingpangolin.com>
synopsis: JSON-RPC servant servers
changelog: |
  # 2.1.1

  * Relax version bounds
  * Remove dependency on `mtl`

  # 2.1.0

  * Relax upper version bounds for `aeson` to `(>= 1.3 && < 1.6)`
  * Relax upper version bounds for `base` to `(>= 4.11 && < 5.0)`
  * Relax upper version bounds for `servant` to `(>= 0.14 && < 0.19)`
  * Relax upper version bounds for `servant-server` to `(>= 0.14 && < 0.19)`

  # 2.0.0

  The previous version was hopelessly broken.  This completely replaces it.

  # 1.0.0

  First release
basic-deps:
  base: '>=4.11 && <5.0'
  servant-server: '>=0.14 && <0.20'
  servant: '>=0.14 && <0.20'
  containers: '>=0.5 && <0.7'
  servant-jsonrpc: '>=1.0.1 && <1.2'
  aeson: '>=1.3 && <2.2'
all-versions:
- 1.0.0
- 2.0.0
- 2.1.0
- 2.1.1
- 2.1.2
author: Ian Shipman <ics@gambolingpangolin.com>
latest: 2.1.2
description-type: haddock
description: Use this package to define a servant server which exposes JSON-RPC over
  HTTP endpoints.
license-name: BSD-3-Clause
