all-versions:
- 0.1.0.0
- 0.1.0.1
author: Chris Martin
basic-deps:
  aeson: ^>=2.0 || ^>=2.1
  base: ^>=4.16 || ^>=4.17
  containers: ^>=0.6.5
  data-default-class: ^>=0.1.2
  quaalude: ^>=0.0
  text: ^>=1.2.5 || ^>=2.0
  unordered-containers: ^>=0.2.17
  vector: ^>=0.12.3
changelog: |
  ## 0.1.0.1 (2023-03-01)

  Support aeson 2

  Support GHC 9.2 and 9.4

  Test framework changed from hedgehog to hspec.

  Examples have moved from doctest comments to the test.

  ## 0.1.0.0 (2018-05-11)

  Initial release
changelog-type: markdown
description: |
  # aeson-decode

  A small and simple library for interpreting JSON after it has been parsed by `aeson` into the `Value` type.

  Decoding failures do not come with any error messages; results are all `Maybe`.
description-type: markdown
hash: e2338408a03c8bb4a55eddfd39c6f716edeaf8177f7511dfd072e2ee79f2ca04
homepage: https://github.com/typeclasses/aeson-decode
latest: 0.1.0.1
license-name: Apache-2.0
maintainer: Chris Martin, Julie Moronuki
synopsis: Easy functions for converting from Aeson.Value
test-bench-deps:
  aeson: ^>=2.0 || ^>=2.1
  aeson-decode: '>=0'
  aeson-qq: ^>=0.8.4
  base: ^>=4.16 || ^>=4.17
  containers: ^>=0.6.5
  data-default-class: ^>=0.1.2
  hspec: ^>=2.8.5 || ^>=2.9 || ^>=2.10
  quaalude: ^>=0.0
  text: ^>=1.2.5 || ^>=2.0
  time: ^>=1.11.1 || ^>=1.12
  unordered-containers: ^>=0.2.17
  vector: ^>=0.12.3
