homepage: https://travis.athougies.net/projects/beam.html
changelog-type: markdown
hash: 68c6c70da019210d2c4b6eaf5ef62980c6a2163d5b19d3daf6f67c4a1994e0b3
test-bench-deps: {}
maintainer: travis@athougies.net
synopsis: SQL DDL support and migrations support library for Beam
changelog: ! "## 0.3.2.0\n\nAdded `haskellSchema` shortcut\n\n## 0.3.1.0\n\nAdd `Semigroup`
  instances to prepare for GHC 8.4 and Stackage nightly\n\n## 0.3.0.0\n\n* Re-introduce
  backend parameter as `Database` type class\n* Move beam migration log schema to
  beam-migrate, since many\n  applications will want to easily manage a database using
  the\n  haskell-based migrations\n* Add `bringUpToDate` and `bringUpToDateWithHooks`
  to\n  `Database.Beam.Migrate.Simple`, which can be used to bring a\n  database up
  to date with the given migration.\n\n## 0.2.0.0  -- 2018-01-20\n\n* First version.
  Released on an unsuspecting world.\n"
basic-deps:
  free: ! '>=4.12 && <5.1'
  bytestring: ! '>=0.10 && <0.11'
  dependent-sum: ! '>=0.4 && <0.5'
  base: ! '>=4.9 && <5.0'
  time: ! '>=1.6 && <1.10'
  unordered-containers: ! '>=0.2 && <0.3'
  text: ! '>=1.2 && <1.3'
  dependent-map: ! '>=0.2 && <0.3'
  pqueue: ! '>=1.3 && <1.5'
  containers: ==0.5.*
  haskell-src-exts: ! '>=1.18 && <1.21'
  ghc-prim: ! '>=0.5 && <0.6'
  mtl: ! '>=2.2 && <2.3'
  beam-core: ! '>=0.7 && <0.8'
  hashable: ! '>=1.2 && <1.3'
  parallel: ! '>=3.2 && <3.3'
  deepseq: ! '>=1.4 && <1.5'
  uuid-types: ! '>=1.0 && <1.1'
  scientific: ! '>=0.3 && <0.4'
  pretty: ! '>=1.1 && <1.2'
  aeson: ! '>=0.11 && <1.5'
  vector: ! '>=0.11 && <0.13'
all-versions:
- '0.2.0.0'
- '0.3.0.0'
- '0.3.1.0'
- '0.3.2.0'
- '0.3.2.1'
author: Travis Athougies
latest: '0.3.2.1'
description-type: haddock
description: ! 'This package provides type classes to allow backends to implement

  SQL DDL support for beam. This allows you to use beam syntax to

  write type-safe schema generation code.

  The package also provides features to introspect beam schemas,

  and support for automatic generation of migrations in SQL and

  Haskell formats.

  This is mostly a low-level support library. Most often, this

  library is used to write tooling to support DDL manipulation in

  your project, or to enable migrations support in beam backends.

  For a more turnkey solution for database migrations, consider

  the <http://hackage.haskell.org/package/beam-migrate-cli beam-migrate>

  command line tool. This provides out-of-the-box support for migrations,

  schema change management, and version control, based on the features

  provided in this library.'
license-name: MIT
