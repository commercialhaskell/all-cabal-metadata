all-versions:
- 0.3.0.0
author: Alexey Kuleshevich
basic-deps:
  base: '>=4.9 && <5'
  bytestring: '>=0.10.4'
  deepseq: '>=0'
  primal: '>=0.2'
  text: '>=1.1.1.3'
changelog: |
  # Changelog for primal-memory

  ## 0.3.0

  * Rename many functions that perform mutation. Add `Mut` infix to operations that deal
    with mutable source memory regions.
  * Rename `resizeMem` -> `reallocMutMem`
  * Export `defaultReallocMutMem`
  * Rename `PrimArray` -> `PArray` and `MPrimArray` -> `PMArray`

  ## 0.2.0

  * Rename `ByteArray` -> `PrimArray`
  * Improve `fromList` conversion
  * Get rid of `allocByteCountMem` in favor of more general `allocMem`

  ## 0.1.0

  * Initial release
changelog-type: markdown
description: |
  # primal-memory


  * Bridge the gap between `Storable` and `Prim`.
description-type: markdown
hash: c1784c750277f5e7ec0486558779d4f2fb2044100f6ce3b02c1817d72873bc34
homepage: https://github.com/lehins/primal
latest: 0.3.0.0
license-name: BSD-3-Clause
maintainer: alexey@kuleshevi.ch
synopsis: Unified interface for memory managemenet.
test-bench-deps:
  QuickCheck: '>=0'
  base: '>=0'
  bytestring: '>=0'
  criterion: '>=0'
  deepseq: '>=0'
  doctest: '>=0.15'
  primal: '>=0'
  primal-memory: '>=0'
  primitive: '>=0'
  random: '>=0'
  template-haskell: '>=0'
