homepage: https://github.com/pjones/playlists-http
changelog-type: markdown
hash: 1a37580fe4ab82b006243176bb7d3983ccb9bf444f37f857139c3e2d2a62a40f
test-bench-deps: {}
maintainer: Peter Jones <pjones@devalot.com>
synopsis: Library to glue together playlists and http-client
changelog: ! "# Version 0.2.0.0 (December 1, 2017)\n\n  * Update to match `playlists`
  0.5.0.0\n\n  * Internally change from `EitherT` to `ExceptT`\n\n# Version 0.1.1.0
  (February 5, 2017)\n\n  * Remove redundant constraints as reported by GHC 8.X.\n\n#
  Version 0.1.0.0 (November 21, 2016)\n\n  * Initial release.\n"
basic-deps:
  http-client: ! '>=0.4 && <0.6'
  exceptions: ! '>=0.8 && <0.9'
  bytestring: ! '>=0.10 && <1.0'
  base: ! '>=4.6 && <5'
  text: ! '>=0.11 && <1.3'
  mtl: ! '>=2.2 && <2.3'
  playlists: ! '>=0.5 && <0.6'
  attoparsec: ! '>=0.10 && <1.0'
  transformers: ! '>=0.4 && <0.6'
all-versions:
- '0.1.0.0'
- '0.1.1.0'
- '0.2.0.0'
author: Peter Jones <pjones@devalot.com>
latest: '0.2.0.0'
description-type: markdown
description: ! "# playlists-http\n\nDownload and parse playlists over HTTP.\n\n##
  Usage\n\nThere are two interfaces, a simple `download` function that runs in\n`IO`
  and a more complicated `download` function that uses `MonadIO`.\n\nSee the following
  modules for more details:\n\n  * `Text.Playlist.HTTP.Simple`\n  * `Text.Playlist.HTTP.Full`\n\nThere
  is also an `examples/example.hs` that demonstrates how to use\nthe `download` function
  which is found in `Text.Playlist.HTTP.Full`.\n"
license-name: BSD3
