all-versions:
- 0.1.0.0
- 0.1.0.1
author: Josh Burgess
basic-deps:
  QuickCheck: '>=2.14 && <2.17'
  acolyte-core: '>=0.1 && <0.2'
  acolyte-server: '>=0.1 && <0.2'
  aeson: '>=2.1 && <2.3'
  base: '>=4.20 && <5'
  bytestring: '>=0.11 && <0.13'
  http-core: '>=0.1 && <0.2'
  http-types: '>=0.12 && <0.13'
  spire: '>=0.1 && <0.2'
  spire-grpc: '>=0.1 && <0.2'
  text: '>=2.0 && <2.2'
changelog: |
  # Revision history for acolyte-test

  ## 0.1.0.1 -- 2026-04-28

  * Bump `cabal-version` to 3.12. Older `cabal-install` (shipped with
    GHC < 9.10) was parsing the file before rejecting it on
    `default-language: GHC2024`, producing PlanningFailed on
    Hackage's GHC 9.8 build matrix row. Bumping forces clean
    rejection on older toolchains. No code changes.

  ## 0.1.0.0 -- 2026-04-27

  * Initial release. Test helpers for acolyte: in-process request
    builders and response assertions that talk directly to a Service
    without spinning up a real network stack.
changelog-type: markdown
description: |-
  Test helpers for APIs built with acolyte. Direct dispatch
  (no network), response assertions, and warp-based integration testing.
description-type: haddock
hash: 065174968230e04b2d1a322764fe4e6fe177dc2fdcc8a11814cd504e29ace1eb
homepage: https://github.com/joshburgess/acolyte
latest: 0.1.0.1
license-name: BSD-3-Clause
maintainer: Josh Burgess <joshburgess.webdev@gmail.com>
synopsis: Testing utilities for acolyte APIs
test-bench-deps:
  acolyte-core: '>=0.1 && <0.2'
  acolyte-grpc: '>=0.1 && <0.2'
  acolyte-openapi: '>=0.1 && <0.2'
  acolyte-server: '>=0.1 && <0.2'
  acolyte-test: '>=0.1 && <0.2'
  aeson: '>=2.1 && <2.3'
  base: '>=4.20 && <5'
  bytestring: '>=0.11 && <0.13'
  http-core: '>=0.1 && <0.2'
  http-types: '>=0.12 && <0.13'
  spire: '>=0.1 && <0.2'
  spire-grpc: '>=0.1 && <0.2'
  spire-server: '>=0.1 && <0.2'
  text: '>=2.0 && <2.2'
